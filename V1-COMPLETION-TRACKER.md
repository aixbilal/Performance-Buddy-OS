# PBOS V1 Completion Tracker

Living execution tracker for turning the decided PBOS V1 into a usable desktop app.
Derived directly from `PBOS-V1-RECONCILIATION-AUDIT.md` — it does **not** add or change requirements.

**States:** `NOT STARTED` · `IN PROGRESS` · `IMPLEMENTED` · `VERIFIED` · `BLOCKED` · `DEFERRED V2+`
A finding is `VERIFIED` only when runtime and/or automated-test evidence exists for it.

**Baseline at start of Batch 0:** Overall V1 38% · Functional 30% · Visual/UX 55%.

---

## Batch Map

| Batch | Scope | Status |
|---|---|---|
| **0** | Foundation lock: SQLite persistence, localStorage migration, native renderer E2E, UI test pattern, dev-control cleanup | **VERIFIED** (commit `59e629a`) |
| **1** | PBOS Spine: Goal → System → Action (engines + CRUD + relational SQLite + tests) | **VERIFIED** |
| **2A** | Academics + Knowledge — user-owned V1 configuration (relational SQLite, CRUD, Course/Topic/Assessment/Attempt, Knowledge Topic/Source/Evidence, Academic↔Knowledge link, honest Notes Hub) | **VERIFIED** |
| 2B | Remaining configuration entities (Development, Fitness, Routines, Money, Book, Block, Capacity) + Quick Capture repair | NOT STARTED |
| **3** | Planner / Calendar / Plan Builder + Today (scheduled blocks, Generate→Review→Apply, capacity editor, collapse model, open-day state) + durable Quick Capture / Capture Inbox | **VERIFIED** (non-native; native recheck deferred to Batch 10 — see Master Batch 2 native row) |
| **4** | Focus & Academic completeness (Normal Study + mode-aware targeting, durable Focus session history + study context, Mastery Check + Result, explicit idempotent Knowledge-evidence handoff) | **VERIFIED** (non-native; native recheck deferred to Batch 10). Past Papers / adaptive testing (`docs/17`) confirmed out of Batch 4 decided scope. |
| **5** | Knowledge & Obsidian V1 slice (vault select, least-privilege filesystem read, markdown index, operational Notes Hub, Topic↔Note links, shared Evidence component, Development↔Knowledge reference + evidence handoff) | **VERIFIED** (non-native; native recheck deferred to Batch 10) |
| **6** | Analytics / Reviews / AI Coach — Weekly + Monthly + Patterns screens, AI Coach Workspace + Permissions, provider abstraction (Fake + Remote + Null), per-domain permissions, minimised context, canonical Recommendation model + durable append-only decision history, Accept/Modify/Reject, deterministic validation, allowlisted safe Apply adapters, re-planning boundary | **VERIFIED** (non-native; native recheck deferred to Batch 10) |
| **7** | Settings (canonical SQLite config + effective-config engine + 4 decided surfaces) / Onboarding (durable first-run state, resumable, real connect-systems, safe reset, existing-user migration) | **VERIFIED** (non-native; native recheck deferred to Batch 10) |
| **8** | Resilience & loading/partial/stale states across all screens + general append-only revision/audit store | **VERIFIED** (non-native; native recheck deferred to Batch 10) — `feat(resilience): complete V1 resilience and revision history` |
| 9 | Global visual normalization (token reconcile, fonts, shared components, App Shell regions) | **VERIFIED — PASS** (`style(ui): normalize V1 visual system and app shell`). Fonts genuinely loaded+applied, canonical tokens, global focus + reduced motion, command-search contrast, Button/Form/Card/Badge/Table normalization, 52 headers → `.t-h2`, 18-route axe sweep 0 critical/serious, responsiveness 1024–1920, text-overflow guards. Collapsed sidebar + P1-11 IA left as explicit product-design decisions. |
| 10 | Full forensic QA + stabilization + release-candidate decision (fresh full gauntlet, workflow/persistence/migration/security/a11y review, bounded native recheck, release-build assessment) | **PASS — RELEASE CANDIDATE = YES** (2026-09-01). Fresh: Vitest 660/660, Playwright 63/63, cargo 101/101, `npm run build` ✓, `cargo build --release` ✓, axe 0 critical/serious. One P2 fixed (dev-only "Simulate relaunch" affordance gated out of production; no test count change — the guard is a build-time concern). Native Tauri E2E stays **BLOCKED — EXTERNAL TOOLCHAIN** (confirmed; no upstream fix). Release *bundling* (identifier/productName/signing/CSP) = operator action. See Master Batch 10 section. |

---

## Batch 0 — Foundation Lock (acceptance detail)

| # | Item | Status | Verification | Tests / Evidence | Deps / Blockers |
|---|---|---|---|---|---|
| 0.1 | SQLite is the authoritative production persistence layer | IMPLEMENTED | pending real-Tauri launch check | `src-tauri/src/db.rs`; `SqliteBackend` chosen when `isTauri()`; `bootstrap.ts` loads from SQLite before render | — |
| 0.2 | Tauri/Rust data-access boundary exists (no SQL in React) | IMPLEMENTED | `cargo build` clean; commands registered | `db::{kv_get_all,kv_set,kv_delete,db_status,migrate_from_localstorage}` in `lib.rs` `invoke_handler`; React only calls `invoke(...)` via `backend.ts` | — |
| 0.3 | Schema / migration mechanism with versioning | IMPLEMENTED | `cargo test` 3/3 | `db.rs` `run_migrations` + `schema_migrations` table; `MIGRATIONS` array; `migrations_are_idempotent` test | — |
| 0.4 | Existing localStorage data has a safe migration path | IMPLEMENTED | `cargo test` + `vitest` | `migrate_from_localstorage` (Rust, transactional, `ON CONFLICT DO NOTHING`) + `migration.ts` `planLocalStorageMigration` + `bootstrap.ts` orchestration | — |
| 0.5 | Migration is tested and idempotent; no duplication; no overwrite of newer data; invalid data reported not reset; version recorded; legacy kept as backup | IMPLEMENTED | `cargo test` `localstorage_migration_is_idempotent_validated_and_non_destructive`; `vitest` `migration.test.ts` (6) | marker row `meta.localstorage_migration`; belt marker `pbos.__sqlite_migrated__`; legacy keys never deleted (cleanup deferred — see note) | — |
| 0.6 | Persistence errors are truthful (loading/loaded/saving/saved/failed; degraded ≠ silent memory) | IMPLEMENTED | `vitest` `bootstrap.test.ts` (degradation path), `usePersistedState.dom.test.tsx` (saved/failed) | `PersistenceStatus` (`phase`, `degradedFrom`, `durable`, `error`); `SaveIndicator` drives off real mirror outcome; `PersistenceStatusLine` shown in Settings → "Data & Storage" | — |
| 0.7 | Dev-only storage-failure control removed from production UI, kept behind a dev mechanism | IMPLEMENTED | grep: no "Simulate Storage Failure" in `src/`; prod bundle has no `__PBOS_DEV__` | removed from `RoutinesOverviewPage.tsx`; `installDevControls()` → `window.__PBOS_DEV__` only under `import.meta.env.MODE === 'development'` | — |
| 0.8 | WebdriverIO can interact with the actual PBOS renderer (launch, see renderer, locate element, interact, navigate 2 routes, assert) | VERIFIED | `npm run test:e2e:tauri` → **2 passing** on a real `webview2 151.0.4129.107` session | `tauri-plugin-wdio` (debug-only registration in `lib.rs`); `withGlobalTauri:true`; `wdio:default` capability; `wdio.conf.ts` external provider (`tauri-driver` + `msedgedriver`) driving the renderer via `browser.tauri.execute`; `wdio/specs/app.e2e.ts` (renders real app · fires real Start button · navigates Focus→Goals · round-trips through Tauri→Rust→SQLite) | `tauri-driver` + `msedgedriver` on PATH |
| 0.9 | Baseline UI/workflow testing pattern established | VERIFIED | `vitest run` + `npm run test:e2e` + `npm run test:e2e:tauri` all green | engine test (existing 17) + React interaction test (`usePersistedState.dom.test.tsx`) + pure async logic tests (`migration/backend/bootstrap.test.ts`) + browser workflow test (`e2e/*.spec.ts`) + native E2E (`wdio/specs/app.e2e.ts`) | — |
| 0.10 | `V1-COMPLETION-TRACKER.md` exists | IMPLEMENTED | this file | — | — |
| 0.11 | Previous tests still pass | VERIFIED | `vitest run` 23 files / 190 tests pass (was 19/172) | — | — |
| 0.12 | Build + lint pass | VERIFIED | `npm run build` ✓, `npm run lint` ✓ (0 errors), `cargo build` ✓ (0 warnings), `cargo test` ✓ | — | — |
| 0.13 | No unrelated domain functionality changed | VERIFIED | `git diff` review — persistence layer + Rust + main.tsx + RoutinesOverviewPage (control removal only) + SettingsPage (status line add) + wdio/e2e config only | — | — |

**Legacy-data cleanup strategy (documented, not yet executed):** after a successful SQLite migration the legacy `pbos:*` localStorage keys are **kept** as a recovery backup. A future maintenance step (earliest Batch 8) will clear them once (a) `db_status().localstorage_migrated` is true and (b) the app has launched cleanly ≥N times, writing a `meta.localstorage_cleanup` marker. Never cleared on the strength of "migration returned success" alone.

---

## Batch 1 — PBOS Spine: Goal → System → Action (acceptance detail)

| Item | Status | Verification / evidence |
|---|---|---|
| Real Goal / System / Action engine, tested | VERIFIED | `performance/engine.ts` (pure; validation, `canTransitionGoal`, relationship resolution, `deriveSystemHealth`/`deriveGoalProgress`/`deriveGoalAttention`). `engine.test.ts` 24 tests. |
| Goals user-creatable / -editable + safe status transitions | VERIFIED | `/goals/new` + `/goals/:id/edit` (`GoalBuilderPage` + shared `GoalForm`); `store.createGoal/updateGoal/transitionGoal`; Playwright + native E2E. |
| One shared Goal Builder for manual + AI-proposed goals; AI never auto-creates | VERIFIED | `GoalBuilderPage` Manual/AI tabs; Accept/Modify only *prefill* the same form (`createdBy: "ai-approved"`); Reject creates nothing. `performance.dom.test.tsx` asserts both paths. |
| Systems user-creatable / -editable; link to 0..n Goals | VERIFIED | `SystemForm` (inline sheet) on Systems Overview + System Detail; `?newFor=<goalId>` creates-and-links from Goal Detail; `store.createSystem/updateSystem/deleteSystem/toggleSystemStar`. |
| Actions user-creatable / -editable; genuine status control; canonical + reused | VERIFIED | `ActionForm` on System Detail; explicit status `<select>` (not a hidden click-cycle), Edit, Delete, keyboard reorder; `capture/store` routes through the same `addAction` shim → `createAction` (no second task type). |
| ONE relationship truth | VERIFIED | `goal_system_links` join table (goal↔system, many-to-many) + `actions.system_id` FK (`ON DELETE SET NULL` → direct commitment). No reverse arrays anywhere. `cargo test` `crud_and_one_relationship_truth`, `repo.test.ts`, native E2E `perf_load` assertion. |
| Derived health not stored as competing state; Unknown ≠ Zero | VERIFIED | `System` has no `healthPercent`/`consistency`/`streak` columns; `deriveSystemHealth([])` → `insufficient-data`, ratio `null`; `deriveGoalProgress` with no metric → `{kind:"none"}`. Engine tests + UI ("Not enough activity yet"). |
| Fresh profile does not depend on seed data | VERIFIED | store initial state `[]`; no `SEED_*` import; `mockData.ts` reduced to a test-only `LEGACY_KV` fixture; honest empty states on both overviews. |
| Relational SQLite persistence for the spine | VERIFIED | `db.rs` migration **v2** (`goals`, `systems`, `actions`, `goal_system_links` + indexes, FKs on); `performance.rs` commands `perf_load/goal_upsert/goal_delete/system_upsert/system_delete/action_upsert/action_delete/link_set/actions_reorder/import_graph`. |
| Legacy performance-data migration — safe, idempotent, non-destructive | VERIFIED | `legacyImport.ts` (pure: parses the Batch 0 `pbos:performance-*` blobs, unions the dueling `Goal.systemIds`/`System.goalId` into one link list, `Action.systemId` wins over `System.actionIds`, drops fabricated consistency/streak numbers, preserves IDs, reports repairs). `perf_import_graph` is `INSERT OR IGNORE` + a `meta.performance_relational_import` marker. `cargo test` `import_is_idempotent_and_non_destructive`, `legacyImport.test.ts` 9, `repo.test.ts`. |
| Created data survives Tauri restart | VERIFIED | native E2E: `perf_reset_for_test` → `location.reload()` → create spine via UI → `perf_load` reads the canonical rows back out of SQLite with correct relationships. |
| Today keeps consuming canonical Actions; no domain rebuilt | VERIFIED | `TodayPage` diff is 3 lines (`"completed"`→`"done"`, `computeSystemHealth`→`systemHealth`, honest "—" for insufficient data). `search/store` diff 1 line (`g.status`→`g.lifecycle`). No other domain touched. |
| Tests / lint / build / cargo / axe | VERIFIED | vitest 27 files / 231 (was 190, +41); lint 0 errors; `npm run build` ✓; `cargo build` debug+release 0 warnings; `cargo test` 6/6; Playwright 4/4 (incl. scoped form axe = 0 critical/serious); native E2E 3/3. |

---

## Batch 2A — Academics + Knowledge (user-owned V1 configuration) (acceptance detail)

| Item | Status | Verification / evidence |
|---|---|---|
| Relational SQLite for Academic + Knowledge | VERIFIED | `db.rs` migration **v3** (`academic_semesters/courses/topics/assessments/attempts`, `knowledge_topics/sources/evidence` + indexes, FKs on). `academic.rs` + `knowledge.rs` data-access; 23 commands wired in `lib.rs`. `cargo test --lib` 18/18. |
| ONE cross-domain link; NO duplicate mastery truth | VERIFIED | `academic_topics.knowledge_topic_id` (FK → `knowledge_topics.id`, `ON DELETE SET NULL`) is the only link, owned by the Academic row. `academic_topics` has no mastery column; the store never persists one; `CourseDetailPage` READS mastery from the linked Knowledge concept's evidence. `cargo test` `academic_topic_links_to_canonical_knowledge_concept_without_copying_mastery`, `crossDomainLink.test.ts`, native E2E `acad_load`/`know_load` assertions (`"masteryPercent" in acadTopic === false`, `masterySelfAssessed === null`, exactly one concept). |
| No score→letter-grade policy; no repeat/replacement policy | VERIFIED | Grades are user-entered letters or NULL; `calculateCGPA` excludes multi-attempt courses and flags `blockedByUnresolvedRepeatPolicy` (docs 13.09/13.10). `cargo test` `no_grade_rule_is_encoded_grades_are_pass_through_or_null`; engine tests preserved. |
| Academic types / deterministic engine / IDs / legacy import / repo / store | VERIFIED | `academic/{types,engine,ids,legacyImport,repo,store}.ts`. Engine adds `validateCourse/Topic/Assessment/AttemptInput` + `analyzeAssessmentWeighting` (empty/under/over — **never rescales**). `engine.test.ts` (verified calc unchanged) + `engine.validation.test.ts` + `legacyImport.test.ts` + `repo.test.ts`. |
| Professor Coverage ≠ Personal Study ≠ Mastery — independently editable | VERIFIED | `store.setProfessorCoverage` / `setPersonalStudyCoverage` are separate ops that touch only their own field; Course Detail exposes an independent `<select>` + number input per topic. `academic.dom.test.tsx` asserts changing one leaves the other untouched and grants no mastery; Playwright `academics.spec.ts`; native E2E. |
| Course CRUD + Course Detail management surface | VERIFIED | `/academics/new` + `/academics/:courseId/edit` (`CourseBuilderPage` + shared `CourseForm`); Course Detail: Edit, Archive/Unarchive, Topic add/edit/delete, Assessment add/edit/delete + inline marks, Attempt+final-grade entry, weighting-config warning. Honest empty state on Academics Overview + `Add Course`. |
| Assessment CRUD + truthful weighting state | VERIFIED | `AssessmentForm`; `analyzeAssessmentWeighting` surfaced as empty/under/over banner. `academic.dom.test.tsx` "add up to 10%" under-weight warning; deterministic weighted score (18/20 @ 100% → 9.0% / 90.0%). |
| Marks / attempts — no auto grade | VERIFIED | inline obtained-marks input (blank = not graded); Attempt card records term + user-chosen letter or "— none —". `store.setAssessmentMarks` / `upsertAttempt`; deterministic weighted-score / SGPA / CGPA unchanged. |
| SGPA/CGPA reads real user data; no seed; no crash on incomplete | VERIFIED | `SgpaCgpaPage` unchanged in design; now consumes the relational store; empty-state row; `activeCourses` filter. |
| Knowledge types / engine / IDs / legacy import / repo / store | VERIFIED | `knowledge/{types,engine,ids,legacyImport,repo,store}.ts`. Persisted `KnowledgeTopic` has **no** mastery field; store projects `Topic` with evidence-derived `masteryPercent` + `hasEvidence`. `deriveTopicView` makes "insufficient evidence" explicit. `engine.test.ts` preserved + `engine.validation.test.ts` + `legacyImport.test.ts` + `repo.test.ts`. |
| Knowledge Topic CRUD + Topic Detail management | VERIFIED | `/knowledge/new` + `/knowledge/:topicId/edit` (`KnowledgeTopicBuilderPage` + `KnowledgeTopicForm` — **no mastery field**); Topic Detail: Edit, derived state/mastery, review-date edit, Evidence list + record, Sources CRUD, linked-Academic-topics readout. Honest empty state + `Add Topic`. |
| Knowledge Source CRUD | VERIFIED | `SourceForm` (add/edit/delete/reassign-to-topic); reference-only, no ingestion claim. `knowledge.dom.test.tsx`; native E2E creates a Source through the UI and reads it from SQLite. |
| Evidence / mastery — moves ONLY from Evidence | VERIFIED | `addEvidence` for a missing topic returns `{ok:false}` (no fake topic, no throw); adding a Source / editing coverage never creates evidence. `knowledge.dom.test.tsx` "mastery moves ONLY from recorded Evidence"; native E2E (80% derived from one recall). |
| Honest Obsidian / Notes Hub | VERIFIED | `/knowledge/notes` (`NotesHubPage`): "Obsidian not connected", explicit "no files have been scanned / no Markdown indexed / no note content stored", lists only reference metadata PBOS already holds. `knowledge.dom.test.tsx` + Playwright assert no fake files. |
| Routes | VERIFIED | `/academics/new`, `/academics/:courseId/edit`, `/knowledge/new`, `/knowledge/:topicId/edit`, `/knowledge/notes` added to `shell/router.tsx`; invalid-id → truthful "not found" (covered by builder pages). |
| Shared form primitives | VERIFIED | `TextField/TextArea/SelectField` promoted to `components/FormFields.tsx`; `performance/formPrimitives.tsx` is a 7-line re-export (ONE impl, no clone). Batch 1 tests + Playwright form-axe still green. |
| Regression — Performance / Today / Focus / Search / Analytics / Language / Onboarding | VERIFIED | zero functional changes to those domains; `useAcademic`/`useKnowledge` back-compat surface preserved (`cgpa`, `courses`, `topics`, `getTopicsForCourse`, `addEvidence`, `assessmentsSaveState`/`evidenceSaveState` aliases…). vitest 307/307, `npm run build` ✓, Playwright `performance.spec.ts` 2/2, native E2E Batch 0/1 specs 3/3. |
| Tests / lint / build / cargo / axe / native E2E | VERIFIED | vitest **36 files / 307** (was 231, +76: 7 domain-test files + `academic.dom` 6 + `knowledge.dom` 7); lint 0; `npm run build` ✓; `cargo fmt --check` (new files) ✓; `cargo test --lib` **18/18**; `cargo build` + `cargo build --release` ✓; Playwright **10/10** (`academics.spec.ts`, `knowledge.spec.ts` incl. Academic↔Knowledge link + 2 scoped-form axe = 0 critical/serious, + smoke + performance); native WebdriverIO E2E **4/4** (new Batch 2A scenario drives Course→Topic→Knowledge concept→evidence→link→assessment→marks→source entirely through the real WebView, then verifies every canonical row + the one relationship straight out of SQLite). |

**Real-user proof (native, through the UI): PASS.** Fresh profile → create "Data Structures" → add "Binary Trees" topic → set Professor Coverage + Personal Study independently (neither grants mastery) → add Quiz 1 + marks (deterministic 90.0%) → create "Binary Trees" Knowledge concept + one recall (80%) → link the Academic topic to it → add a Source. `acad_load`/`know_load` confirm: course + topic + assessment persisted; `academic_topics.knowledge_topic_id` set; no mastery value on the Academic side; exactly one Knowledge concept with one evidence + one source.

---

## Master Batch 2 — Development + Fitness + Routines + Reading/Language + Money

**Status: COMPLETE — ACCEPTED by the user (2026-08-31). Committed as `feat(config): complete user-owned V1 domain configuration`.**
All 8 Batch 2 product domains (Academics + Knowledge in `9a09ddf`; Development, Fitness, Routines, Reading/Language, Money in this commit) are relational, seed-free, and operable end to end, verified by unit + engine + repository + Rust + React-Testing-Library + Playwright + SQLite evidence. The user **explicitly accepted** that body of evidence as sufficient to close the batch, with native Tauri desktop E2E recorded as an externally-blocked verification item (below). System WebView2 runtime is **not** to be downgraded; the native blocker must stay documented and be rechecked during final QA / Batch 10 (or earlier if an upstream-compatible `@wdio/tauri-service` release appears).

**NATIVE TAURI E2E: BLOCKED — EXTERNAL TOOLCHAIN.** `@wdio/tauri-service@1.3.0` / WebView2 151.x compatibility issue. All 9 native scenarios (Batch 0 renderer + SQLite, Batch 1 Goal→System→Action, Batch 2A Academic↔Knowledge, Development, Fitness, Routines, Reading/Language, Money) are written in `wdio/specs/app.e2e.ts` and ready to run once resolved.

| Area | State | Evidence |
|---|---|---|
| Migration **v4** | DONE | `db.rs` `CURRENT_SCHEMA_VERSION = 4` — Development, Fitness *and* Routine tables + indexes + FKs (one migration for the batch, per the v3 precedent). `cargo test` `migrations_are_idempotent` covers v1→v4 on a fresh DB. |
| **Development** — `development.rs` + TS types/engine/ids/legacyImport/repo/store + UI (Overview, Project Detail, Skill Detail, Learning Path, Project/Skill builders) + routes | DONE + browser-verified | Rust `cargo test` +5 (cascade, `evidence_percent_is_not_a_column`, FK-reject, dangling-safe import, created_at immutability); TS +32 (`engine.validation` 8, `legacyImport` 8, `repo` 10, `development.dom` 6); Playwright `development.spec.ts` (workflow proving Project progress ≠ Skill capability, unreviewed-AI evidence excluded; + scoped-form axe = 0 critical/serious). `project_skill_links` many-to-many (no `skillIds[]`); `evidencePercent` derived, never stored. |
| **Fitness** — `fitness.rs` + TS types/engine/ids/legacyImport/repo/store + UI (Overview, Training Plan Detail, Active Workout, Recovery/Readiness, Plan builder) + routes | DONE + browser-verified | Rust `cargo test` +6 (BASE PLAN untouched by an actual session, plan-delete keeps workout history via SET NULL, FK-reject, `no_readiness_column_exists`, dangling-safe import, created_at immutability); TS +27 (`engine.validation` 8, `legacyImport` 7, `repo` 8, `fitness.dom` 4); Playwright `fitness.spec.ts` (workflow proving an ACTUAL workout that differs from the prescription leaves the BASE PLAN byte-for-byte unchanged; readiness honest `insufficient-data`; + scoped-form axe = 0 critical/serious). `deriveReadiness` preserved; Today's Prescription is engine-advisory only (no table); legacy prescriptions dropped on import; no readiness/history fabricated. |
| **Routines** — `routine.rs` + TS types/engine/ids/legacyImport/repo/store + UI (Overview, Routine Detail, Routine Builder, Daily Check-In) + routes | DONE + browser-verified | Migration v4 `routines` table **extended** (uncommitted-batch amendment, no v5): added a semantic cadence — `schedule_type` (`daily`/`weekly-days`/`times-per-week`), `schedule_days` (JSON int array), `schedule_target` — plus `paused`. Rust `cargo test` +8 (log CASCADE on routine delete, **System delete → `related_system_id` SET NULL, routine kept**, dangling System link stored as NULL not an FK error, `no_consistency_or_streak_column_exists`, FK-reject log, dangling-safe idempotent import, created_at immutability, log upsert = one canonical row per id). TS +40 (`engine` 18 — schedule-aware `deriveRoutineConsistency`: expected-vs-completed, today-not-yet-a-miss, unscheduled days excluded, rest/skipped excused, brand-new = "no history yet" not 0%, times-per-week per-ISO-week; `engine.validation` 12; `legacyImport` 8; `repo` 7; `routine.dom` 6). Playwright `routine.spec.ts` ×3 (create → configure cadence → check-in → **idempotent update = one canonical log/day** → reload persists → pause hides from Daily Check-In but keeps the routine; optional canonical System link referenced-not-duplicated + unlink; scoped-form axe = 0 critical/serious). Consistency/streak derived, never stored; no streak counter anywhere; a check-in writes only a `RoutineLog`, never an Action. Back-compat kept for Analytics (`getConsistency`), Search (`routines`) and Language (`setTodayState` / `deriveCompletionState`); Language handles an absent seeded routine truthfully (no fake routine recreated). |
| **Reading & Language** — `language.rs` (Language + Reading, one module) + TS types/engine/ids/legacyImport/repo/store + UI (Overview, Language Path Detail, Learning Session, Book Detail, Path/Book builders) + routes | DONE + browser-verified | Migration v4 **extended** (uncommitted-batch amendment, no v5): `language_paths` / `language_units` / `language_sessions` / `books` / `reading_sessions` + indexes + FKs. Cross-domain links are references only, SET NULL on delete: `language_paths.related_routine_id`→`routines`, `language_units.knowledge_topic_id`→`knowledge_topics`, `books.knowledge_topic_id`→`knowledge_topics`. `books.note_ref` is free text — no Obsidian read/index. Rust `cargo test` +9 (path→units/sessions CASCADE; unit delete → session `unit_id` SET NULL, session kept; **routine + knowledge links cleared when the target is deleted**; dangling link stored as NULL not FK error; `no_mastery_or_progress_column_on_path_or_book`; `recall_score_is_optional_minutes_alone_are_not_mastery`; FK-reject unit; dangling-safe idempotent import; created_at immutability). TS +36 (`engine` 8 — `derivePathProgress` null-not-0% for no units, `deriveReadingProgress` null-not-0% for unknown total + clamp, `deriveNextUnit`, `deriveSessionEffects` no-evidence-without-recall; `engine.validation` 13; `legacyImport` 5 — synthesizes 1 path/language, remaps lessons→units keeping ids, drops orphan lessons, no fabricated sessions; `repo` 8; `language.dom` 8). Playwright `language.spec.ts` ×4 (path → units → learning session advances curriculum progress only → **Knowledge stays empty** → reload persists; optional Routine link referenced-not-duplicated + unlink; reading: deterministic percent, **UNKNOWN total ≠ 0%**, reading session advances the page but records no mastery; scoped-form axe ×2 = 0 critical/serious). Path/reading progress derived, never stored; a learning session marks its unit complete (mechanical) but a Knowledge-evidence record is created only on an **explicit** user action after a real recall check. `useLanguage()` API redesigned — the only consumer (`ReadingLanguageOverviewPage`) was rewritten; no other domain imports it. |
| **Money OS** — `money.rs` + TS types/engine/ids/legacyImport/repo/store + UI (Overview, Transactions, Budget & Savings, Insights & Review) + routes | DONE + browser-verified | Migration v4 **extended in place** (no v5): `money_savings_goals` / `money_transactions` / `money_planned_expenses` / `money_budgets` + indexes + FKs. Product locks in the shape: `money_transactions.type` distinguishes `savings-transfer` (engine excludes it from every spending/budget total); `money_planned_expenses` is a separate table linking to the realising transaction via `transaction_id` (SET NULL; the planned row is never rewritten into an expense, and reverts to "upcoming" if that transaction is deleted); savings-goal progress = `opening_amount` (user-entered) + linked savings-transfer transactions — **no stored `current_amount`**; **no `bank_verified` / `score` / `performance` column anywhere**. Rust `cargo test` +8 (transfer keeps its own type + goal link; delete goal → transfers survive, link SET NULL; delete transaction → planned expense survives + link cleared; dangling link stored NULL not FK error; `no_bank_verification_or_performance_score_column_anywhere`; FK reject; dangling-safe idempotent import; created_at immutability). TS +34 (`engine` 10 — transfer ≠ expense in every total, budget usage from actual expenses only + `hasData` unknown-≠-0, savings = opening + linked transfers never expenses, planned never in a spend total, insights deterministic + no bank-balance claim + no advice; `engine.validation` 12; `legacyImport` 6 — legacy `currentAmount`→`openingAmount` reported, planned expenses never converted to actual; `repo` 7; `money.dom` 8). Playwright `money.spec.ts` ×2 (income 50k + expense 10k + transfer 15k → **Spent = 10,000 not 25,000**, tracked net 25,000; planned expense does NOT raise Spent; "Record actual" creates a **separate** transaction; budget usage 4,000/5,000 from the actual expense only, income + transfer ignored; savings goal progress = opening + transfers; edit → reload persists; Insights say "not a verified bank balance"; scoped-form axe = 0 critical/serious). Back-compat kept for Analytics (`balance` / `expenseTotal`), Quick Capture (`addTransaction`), Onboarding (`transactions`). |
| Suite | vitest **56 files / 477** (was 52/443; +4 money test files, `money/engine.test.ts` rewritten relational); lint 0 (money files carry only the same `only-export-components` warning every domain store/form has); `npm run build` ✓; `cargo test --lib` **54/54** (was 46); `cargo build` + `--release` ✓; `cargo fmt`/`clippy` clean for all new Batch 2 Rust modules (`money.rs`, `language.rs`, `routine.rs`, `development.rs`, `fitness.rs`, `db.rs`, `lib.rs` — the only `clippy` warning is the pre-existing `performance.rs` `usize→usize`; the pre-existing `build.rs`/`main.rs`/`performance.rs` rustfmt churn is untouched); Playwright **24/24** (Development ×2 + Fitness ×3 + Routines ×3 + Reading/Language ×4 + Money ×2 + all pre-existing). |
| **Master Batch 2 seed-removal audit** | CLEAN | Runtime grep: no `SEED_` / `mockData` import in any of the 8 Batch 2 product domains (academic, knowledge, development, fitness, routine, language, money). The only stores still on `usePersistedState` + `SEED_*` are `intelligence`/AI Coach (Batch 6) and `settings` (not a Batch 2 configuration domain) — all out of scope. (`planning` and `capture` moved to relational SQLite in Batch 3.) Every Batch 2 domain's `mockData.ts` is now `FIXTURE_*`, imported only from `*.test.*`. |
| **Native WebdriverIO / Tauri E2E — BLOCKED (environment)** | `npm run test:e2e:tauri` fails **every spec including the 3 pre-existing green Batch 0/1/2A ones**, with `@wdio/tauri-service`: "Tauri plugin not available". A full working-tree revert to the last-green commit `9a09ddf` (Rust `lib.rs`+`db.rs` *and* the entire frontend, 145 modules) **still fails identically** — proving the regression is not Batch 2B code. The debug `app.exe` launches + migrates cleanly standalone; `browser.execute` no longer reaches the app's DOM frame so `window.wdioTauri` is never seen (frontend log capture is 0 bytes). Most probable: a silent WebView2 Runtime / Edge background auto-update changing the automation surface `msedgedriver` + `browser.execute` rely on. A config-pin attempt (`autoDownloadEdgeDriver: false` + `edgeDriverPath`) made it worse ("Failed to create a session") and was reverted. **End-of-session recheck (2026-08-31, after connectivity was restored):** still BLOCKED — all 7 native specs (incl. the 3 pre-existing Batch 0/1/2A) fail; the WebDriver session now establishes but every `browser.execute` / `execute/async` throws `WebDriverError: invalid session id` and the session dies mid-run (`[WDIO][Frontend] Timeout waiting for Tauri`). Same `msedgedriver` 151.0.4129.107 / WebView2 automation-surface regression; not a network issue, not Batch 2B code. Native Batch 2 scenarios for Development + Fitness + **Routines** + **Reading/Language** are written in `wdio/specs/app.e2e.ts` (the Reading/Language one drives path → unit → learning session → book-with-total through the UI, then asserts from `lang_load`: one path with `relatedRoutineId` null and no stored progress/mastery column, the session's linked unit `completed` mechanically, `recallScore` null, and the book's `currentPage`/`totalPages`) and `db_status().schema_version` stays 4 (v4 extended in place, not a new migration) — ready to run once the bridge is restored. **End-of-session recheck (Reading/Language session):** still BLOCKED — all 8 native specs fail with `@wdio/tauri-service`: "Tauri plugin not available". **Bounded toolchain investigation (Money session, 2026-08-31):** `claude doctor` → "No installation issues found" (the Claude Code auto-updater's `install_failed` is a separate, cosmetic issue — the CLI runs fine, and nothing connects it to the Tauri bridge). Native toolchain audit: WebView2 Runtime **151.0.4129.107**, msedgedriver **151.0.4129.107** — an **exact match** (so it is *not* a driver/runtime version mismatch); `tauri-driver` **v2.0.6** (latest); `@wdio/tauri-service` **1.3.0** and `@wdio/tauri-plugin` **1.3.0** — both already the **latest published versions** (`npm view` confirms nothing newer exists). There is no version bump available and no reversible local change: the remaining options are a system-wide WebView2 Runtime downgrade (explicitly out of scope; Edge would re-update it) or an upstream `@wdio/tauri-service` release supporting WebView2 151. **Root cause:** upstream incompatibility between `@wdio/tauri-service@1.3.0` (latest) and WebView2 Runtime 151.x — `browser.execute` / `execute/async` can no longer reach the renderer frame through the CDP surface WebView2 151 exposes. The 9 native scenarios (Batch 0 renderer + SQLite, Batch 1 Goal→System→Action, Batch 2A Academic↔Knowledge, Development, Fitness, Routines, Reading/Language, Money) are all written in `wdio/specs/app.e2e.ts` and ready to run once an upstream fix lands. `db_status().schema_version` stays 4 (v4 extended in place across the whole batch, never a new migration). **Final recheck (Money session, 2026-08-31):** run once more after the full toolchain audit — identical result (`0 passed, 1 failed`, "Tauri plugin not available", WebView2 151.0.4129.107). **USER DECISION (2026-08-31):** the browser + RTL + repository + Rust + SQLite evidence is **explicitly accepted** as sufficient to close Master Batch 2; native Tauri E2E is recorded as **BLOCKED — EXTERNAL TOOLCHAIN** (`@wdio/tauri-service@1.3.0` / WebView2 151.x). System WebView2 runtime must **not** be downgraded. This entry stays open and must be rechecked during final QA / Batch 10, or earlier if an upstream-compatible `@wdio/tauri-service` release appears. |

---

## Master Batch 3 — Planner / Calendar / Plan Builder / Today + durable Quick Capture

**Status: COMPLETE — non-native evidence green (2026-08-31). Committed as `feat(planning): complete V1 planner calendar and capture loop`.**
The PLAN stage of the operating loop is now operable end to end: an Action is scheduled into a canonical Planning Block, the Calendar Week renders it, Today surfaces it grouped by time, the Action stays Action-owned through completion, and the planning history survives. Quick Capture is durable and delegates every confirmed capture to the existing canonical domain engine. Native Tauri desktop E2E remains **BLOCKED — EXTERNAL TOOLCHAIN** (accepted in Master Batch 2; rechecked at Batch 10).

| Area | State | Evidence |
|---|---|---|
| Migration **v5** | DONE | `db.rs` `CURRENT_SCHEMA_VERSION = 5` — `planning_blocks`, `planning_capacity` (single-row config), `capture_inbox` + indexes + FKs. v1–v4 bodies **untouched** (only the version constant changed). `cargo test` `migrations_are_idempotent` covers v1→v5 on a fresh DB. |
| **Planning** — `planning.rs` + TS `types/engine/ids/legacyImport/repo/store` + `PlannerPage` + `CalendarWeekPage` + routes | DONE + browser-verified | `planning.rs` (`plan_load/block_upsert/block_delete/capacity_set/import_graph/reset_for_test`, registered). One link: `planning_blocks.action_id` → `actions.id` `ON DELETE SET NULL` — an Action has 0..n blocks; deleting it keeps history with the link nulled (Rust `block_links_to_a_canonical_action_and_survives_its_deletion`). Block `status` is block-local (`no_action_status_or_completion_column_on_a_block`); `locked` + `source` (`manual`/`generated`) persisted. Engine preserved (13 tests) + civil-date math (`startOfWeekIso`/`weekDates`/`addDaysIso`/`mondayIndexOf`, no timezone drift) + deterministic `proposeSchedule`. TS repo (Sqlite + LocalRepo), seed-free store, one-time legacy import. Tests: `cargo test` +6, vitest `engine.test` (24), `repo.test` (7), `legacyImport.test` (6), `PlannerPage.dom` (5), `CalendarWeekPage.dom` (5). |
| **Planner operational** — Plan Builder, capacity editor, Generate→Review→Apply | DONE + browser-verified | Plan Builder (`FormFields`-based, scoped axe 0 critical/serious): title, optional Action link, weekday **or** absolute date, start/end, fixed/flexible; pre-save `checkFit` → explicit **"Could Not Fit"** (conflict *or* capacity), invalid input never saved. "Schedule today" one-click for unscheduled Actions. Capacity editor exposes the canonical `planning_capacity` (Planning is the single truth). Generate produces a **proposal** (deterministic, labelled no-AI) that mutates nothing until **Apply**; Apply replaces only generated-unlocked blocks — **manual and locked blocks survive** (`PlannerPage.dom` + Playwright `e2e/planning.spec.ts`). |
| **Calendar Week** | DONE + browser-verified | `CalendarWeekPage` is a **view** over the canonical store — no `CalendarEvent` record. 7-day grid, prev/next/this-week nav (labelled), dated blocks on their exact date, undated blocks recur by weekday, conflict highlight, capacity shown **separately** from conflicts, fixed/flexible/locked/linked indicators, deterministic block detail panel (inspect linked Action live, lock/unlock, move, delete). Deleted-Action block shows honestly as history and never crashes. `CalendarWeekPage.dom` (5) + Playwright. |
| **Today integration** | DONE + browser-verified | Reads `usePlanning().todaysBlocks` (no second array). Blocks grouped **NOW / NEXT / LATER / EARLIER** by current time; honest **open-day state** ("Your day is open" + Open Planner / Open Calendar — never "you failed to plan"); a linked block shows the Action's **live** status and a "Start Focus" entry (starting Focus never completes the Action); on Action completion Today reflects it and the planning block/history remains. `TodayPage.dom` (4) + Playwright. |
| **Quick Capture / Capture Inbox** | DONE + browser-verified | Durable `capture_inbox` (`capture.rs`, registered). Palette flow: type → see proposed classification → **Confirm** (routes to the real domain engine) / Change type / Keep in Inbox — high-confidence captures need no navigation; ambiguous → durable inbox, no input loss. `/capture-inbox` route added (was 404). Confirm delegates to `addAction` / `createTransaction` / `checkInRoutine`; a resolved capture cannot be confirmed twice (no duplicate writes). "note" keeps the honest V1 boundary (no Knowledge concept, no mastery, no fake Obsidian write). `capture/repo.test` (6), `capture/store.dom.test` (6: Action/Expense/Routine delegation + no-duplicate + reload persistence + no-match stays unresolved), `CaptureInboxPage` classify/confirm/dismiss/delete UI, Playwright `e2e/planning.spec.ts` (Action + Expense + Routine + ambiguous-survives-reload). |
| **Academic Event capture** | DEFERRED (documented) | The V1 Academic model (Course/Semester/Topic/Assessment/Attempt) has **no calendar-event entity**, and inventing one is out of scope. An academic-flavoured capture is kept in the durable Capture Inbox for manual classification — the honest boundary, consistent with the "note" decision. Revisit if/when an Academic scheduling entity is decided. |
| **Nav / routing** | DONE | `/calendar` now = **Calendar Week** (the decided screen), `/planner` = Planner Overview + Plan Builder; both added to the sidebar "core" group. The broader IA question (is Systems top-level) is still the open product decision below — not resolved here. |
| **Seed removal** | CLEAN | `planning/mockData.ts` is `FIXTURE_*` (test-only) + UI constants; `planning`/`capture` stores start empty and no longer use `usePersistedState`/`SEED_*`. Fresh profile: no planning blocks, empty inbox (repo tests assert). |
| Suite | vitest **60→63 files / 502→526** (+24: planning engine/repo/legacyImport/dom, calendar dom, today dom, capture routine); `cargo test --lib` **64/64**; Playwright **24→28** (+4 `e2e/planning.spec.ts`: plan→calendar→today, conflict≠capacity + locked-survives-regenerate, quick-capture ×4 types, Planner-form scoped axe); `npm run build` ✓; `npm run lint` 0 errors; `cargo build` + `--release` ✓; `cargo fmt`/`clippy` clean for `planning.rs`/`capture.rs`. |
| **Native WebdriverIO / Tauri E2E** | BLOCKED — EXTERNAL TOOLCHAIN | Same `@wdio/tauri-service@1.3.0` / WebView2 151.x incompatibility accepted in Master Batch 2. Not investigated this batch (per brief). Batch 3 native scenarios join the written-but-unrunnable set; rechecked at Batch 10. |

---

## Master Batch 4 — Academic study, Focus & Mastery

**Status: COMPLETE — non-native evidence green (2026-08-31). Committed as `feat(academics): complete V1 study focus and mastery loop`.**
The Academic execution loop is operable end to end: COURSE → TOPIC → NORMAL STUDY → FOCUS → MASTERY CHECK → EVIDENCE → KNOWLEDGE MASTERY, with the three-way distinction (Professor Coverage ≠ Personal Study ≠ Mastery) and the Focus-time ≠ mastery rule held throughout, proven by unit + engine + RTL + Playwright + Rust/SQLite evidence. Native Tauri desktop E2E remains **BLOCKED — EXTERNAL TOOLCHAIN** (rechecked at Batch 10).

| Area | State | Evidence |
|---|---|---|
| Migration **v6** | DONE | `db.rs` `CURRENT_SCHEMA_VERSION = 6` — `focus_sessions` (durable session history + optional course/academic-topic/knowledge-topic/action/block context links, all SET NULL on delete) and `mastery_checks` (personal self-check + result + JSON `items` + `evidence_id` idempotency guard). v1–v5 bodies **untouched** (only the version constant changed). `migrations_are_idempotent` covers v1→v6. |
| **Study module** — `study.rs` + Rust tests | DONE | `study.rs` (`study_load`, `study_focus_session_upsert/delete`, `study_mastery_check_upsert/delete`, `study_mastery_link_evidence`, `study_import_graph`, `study_reset_for_test`), registered in `lib.rs`. `cargo test` +7: session persists with optional context and **no mastery column**; deleting a linked Knowledge topic SET NULLs the session link, session survives; `mastery_checks` has **no** academic-mastery/grade column; **evidence handoff is set-once, never overwritten** (a second link attempt with a different id returns the first); deleting the linked evidence SET NULLs the check's link, check survives; a full check re-upsert never touches `evidence_id`; import idempotent. |
| **Focus (extended, not replaced)** | DONE + browser-verified | `focus/types.ts` adds context links + a persisted `FocusSessionRecord`; `focus/repo.ts` (SQLite + LocalRepo) persists completed sessions; `focus/store.tsx` adds `startWith(ctx)`, a durable `history`, `getSessionsForKnowledge/AcademicTopic`, and `reset`. The deterministic engine + its 10 tests are unchanged; `deriveFocusCompletionEffects` still yields Knowledge evidence **only** when a real recall score is entered. `FocusPage` shows the linked study context, a "Back to study" return, and a durable "Recent Sessions" list (closes the audit's "Focus completion has no on-page history"). `focus/store.dom.test.tsx` (+3): `startWith` context + persistence + remount reload; finish-without-recall writes **no** evidence; finish-with-recall + linked topic writes exactly one `recall` evidence. |
| **Normal Study** — `/academics/study` | DONE + browser-verified | `studyEngine.ts` (pure): explainable **reason codes** (`review-due`, `professor-covered-not-studied`, `evidence-weak`, `no-evidence`, `in-progress`, `not-started`) ordered by a small fixed predicate list per mode — **no universal priority score** (`docs/14.04` marks weights RESEARCH REQUIRED). `NormalStudyPage` reads real Courses/Topics/coverage + linked Knowledge state (`isReviewDue`); honest empty states (no courses / no topics); "Mark studied" updates **Personal Study only** via an explicit 0/25/50/75/100 control (never a timer side-effect); "Start Focus" hands the canonical Focus session the course/academic-topic/knowledge-topic context + a return path; "Start Mastery Check" opens a check. `studyEngine.test.ts` (5) + `study.dom.test.tsx`. |
| **Study modes** | DONE | Reuses the **canonical** `useSettings().mode` (`normal` / `midterm` / `final` / `recovery`) — no competing mode store. The Study page maps to Normal / Exam / Recovery and changes **ordering and which topics surface only**; `study.dom.test.tsx` + `e2e/study.spec.ts` assert churning modes mutates no Course/Topic/evidence data, and Exam surfaces professor-covered-but-not-nailed topics first while Recovery lists only the weakest (smallest useful restart). |
| **Mastery Check + Result** — `/academics/mastery`, `/academics/mastery/:checkId` | DONE + browser-verified | `masteryTypes.ts` / `masteryEngine.ts` (deterministic self-check: fixed prompt set, `confident/partial/unsure` → score, neutral outcome bands `needs-reinforcement`/`developing`/`strong` with advisory review interval — **no fake question bank, no "failed" language**), `masteryRepo.ts`, `masteryStore.tsx` (`MasteryProvider`: `startCheck`, `submitCheck` with validation, `recordEvidence`). One page runs the in-progress check (fieldset + radio groups) and shows the completed Result (score, prompt-by-prompt, weak-spot guidance, next-review suggestion, evidence panel). `masteryEngine.test.ts` (13) + `study.dom.test.tsx`. |
| **Knowledge evidence handoff** | DONE + browser-verified | `recordEvidence` is explicit and **idempotent**: it delegates to canonical `useKnowledge().addEvidence` (`type: "quiz"`, `title: "Mastery Check — <topic>"`), then `study_mastery_link_evidence` sets `evidence_id` set-once and returns the effective id; a losing race deletes its duplicate evidence so exactly one survives. `study.dom.test.tsx` + `e2e/study.spec.ts`: completing an all-confident check → record → **one** Knowledge evidence row → Knowledge mastery = 100% (derived, not stored); button disappears, re-record (UI and programmatic) adds nothing; reload keeps one row. A low (all-unsure) result records neutral 0/4 evidence with **no** grade / coverage / prior-evidence change. |
| **No-Knowledge-link state** | DONE | A Mastery Result for an unlinked Academic Topic shows an explicit "no linked Knowledge concept" panel with options (link / create via topic detail) and **no** Record button — `study.dom.test.tsx` asserts nothing is invented (`know.topics` stays empty). |
| **Focus completion semantics** | DONE + browser-verified | `e2e/study.spec.ts` "Normal Study → Focus": Start Focus from a study target → canonical session with academic context → Finish with no recall → "NO mastery evidence added", session logged as "activity only", Professor Coverage still `taught`, linked Knowledge concept still "mastery unknown"; session history survives a hard reload. |
| **Past Papers / adaptive testing** | DEFERRED (documented) | `docs/17` (Adaptive Testing & Past Papers) is a separate phase and is **not** in the Batch 4 decided scope (the audit remediation plan for Batch 4 lists only Normal Study + Mastery Assessment/Results + Course Detail depth). No `past_papers` tables or UI added. Revisit in a later batch. |
| **Regression** | CLEAN | No functional change to Performance / Planning / Calendar / Today / Quick Capture / Development / Fitness / Routines / Language / Reading / Money / Analytics / Search / Onboarding. Core invariants hold: Professor Coverage ≠ Personal Study ≠ Mastery; Academic Assessment ≠ Mastery Check; Focus Time ≠ Mastery; Planning Block ≠ completion; Knowledge Evidence owns mastery truth. The Focus store's old hardcoded `linkedTopicId: "topic-binary-trees"` demo default was removed (fresh session now links nothing). |
| Suite | vitest **63→67 files / 526→550** (+24: `studyEngine` 5, `masteryEngine` 13, `study.dom` 8, `focus/store.dom` 3, minus refactor churn); `cargo test --lib` 64→**71** (+7 `study.rs`); Playwright **28→32** (+4 `e2e/study.spec.ts`: Study→Focus (time≠mastery), Mastery→one-idempotent-evidence, mode-switch-no-mutation, Mastery-form scoped axe); `npm run build` ✓; `npm run lint` 0 errors; `cargo build` + `--release` ✓; `cargo fmt`/`clippy` clean for `study.rs`. |
| **Native WebdriverIO / Tauri E2E** | BLOCKED — EXTERNAL TOOLCHAIN | Unchanged external `@wdio/tauri-service@1.3.0` / WebView2 151.x issue; not investigated (per brief). Batch 4 native scenarios join the written-but-unrunnable set; rechecked at Batch 10. |

---

## Master Batch 7 — Settings + Onboarding + first-run lifecycle

**Status: COMPLETE — non-native evidence green (2026-08-31). Committed as `feat(setup): complete V1 settings and onboarding lifecycle`.**
The entry/configuration lifecycle is durable end to end: first launch → cinematic splash → Welcome → Personal Setup → Connect Systems → Review → Launch → Today; a normal launch is splash → Today; an interrupted setup resumes at the saved step with input intact; a migrated existing user is never forced back through first-run. Settings is now a single canonical SQLite config with a deterministic Base → Mode → Temporary → Effective engine. Native Tauri desktop E2E remains **BLOCKED — EXTERNAL TOOLCHAIN** (rechecked at Batch 10).

| Area | State | Evidence |
|---|---|---|
| Migration **v9** | DONE | `db.rs` `CURRENT_SCHEMA_VERSION 8 → 9` — `onboarding_state` (single row `CHECK id=1`: status / current step / `first_boot_experience_seen` / flow version / entered personal setup JSON / system choices JSON / timestamps) and `settings_config` (single row: base config JSON + active mode + temporary overrides JSON + notifications/appearance JSON — **no `effective` column**, Rust `settings_config_round_trips_as_one_row_with_no_effective_column`). v1–v8 bodies **untouched**; `migrations_are_idempotent` covers v1→v9. |
| **`setup.rs` module + Rust tests** | DONE | `setup_load` (returns onboarding + settings + `existing_user_marker`), `setup_onboarding_upsert`, `setup_settings_upsert`, `setup_reset_onboarding` (touches ONLY `onboarding_state`), `setup_reset_for_test` (debug-only). `existing_user_marker` = the deterministic `meta.localstorage_migration` key — an established profile migrated from an earlier build is treated as already-onboarded, never forced through first-run (§28), and this is **not** guessed from entity counts. `cargo test` +5: fresh DB has no rows + no marker; onboarding is one round-tripping row; **reset clears only onboarding — Settings + a seeded Knowledge topic survive**; settings is one row with no effective column; the marker follows the meta key. |
| **Settings — canonical persistence** | DONE + browser-verified | `settings/repo.ts` (`SettingsRepo` — SQLite via `setup_settings_upsert` / one localStorage row) + `settings/store.tsx` reworked: **`usePersistedState` and `SEED_*` removed** (no competing authority), a one-time import of the legacy `pbos:settings-*` keys, `saveState`. `settings/mockData.ts` deleted. `settings/store.dom.test.tsx` (6): base-config change persists across a remount; a save failure keeps the in-memory value; restore-interface-defaults resets notifications+appearance only (mode/base untouched — `neverAffects` still lists `aiPermissions`). |
| **Effective-config engine** | DONE + browser-verified | `settings/engine.ts` adds `resolveEffectiveConfig(config, now)` → `{weekdayAcademicCapacityMinutes, protectedSleepHours, precedence:{base, modeDelta, temporaryDelta, activeTemporaryOverrides}}` — **temporary > mode > base**, deterministic, non-destructive: computing the value never mutates base/mode/overrides, an expired temporary override drops out automatically, and with no mode + no active override effective === base. `settings/engine.test.ts` +7 (incl. the handoff's own 90+45+15=150 worked example, and "clear temporary restores the mode/base value"). `e2e/setup.spec.ts` drives the full precedence dance (90 → midterm 135 → +temp 150 → clear 135 → recovery 60; base stays 90) and a reload proves the mode choice persists. |
| **Settings surfaces** | DONE + browser-verified | One `/settings` page with the decided sections: **Overview** (real status lines), **Performance & Planning** (mode buttons, live effective-config trace with `data-testid`, add/clear temporary overrides, base protected-sleep + weekday-capacity, a link out to the Planner for the canonical scheduling capacity — not duplicated), **AI / Privacy / Data** (provider status + enable/disable from the canonical AI Coach store, Obsidian vault status from the canonical Obsidian store, a local-first / "no key stored" / permissions explainer, links to `/ai-coach/permissions` and `/knowledge/notes`, and **Reset onboarding** with a confirm step), **Notifications** (master + per-category, honest "no OS notifications yet"), **Appearance** (reduced motion + density, honest "single dark theme in V1"), **Data & Storage** (`PersistenceStatusLine`, restore interface defaults). No fake "configured" badges; no second AI-permission / vault-path / planning-capacity store. |
| **Onboarding — durable state** | DONE + browser-verified | `onboarding/repo.ts` + `onboarding/store.tsx` reworked from memory-only to a durable single row. First-load logic: a stored row is authoritative; else the existing-user marker → seed `completed` once; else a genuinely fresh profile → `not_started` (persisted on first interaction). `onboarding/engine.ts` adds `getPrevStep` / `stepIndex` / `deriveInitialOnboardingStatus`. `onboarding/store.dom.test.tsx` (6): fresh requires onboarding; step + entered data persist and a remount resumes exactly there; Launch writes the entered baseline into the **canonical Settings** store (`applyOnboardingBaseline` — one atomic commit); completion is idempotent (remount does not reopen it); Connect Systems reflects real domain status; reset returns to first-run without deleting the Knowledge topic or the configured Settings. |
| **Onboarding wizard UI** | DONE + browser-verified | `OnboardingPage.tsx` rewritten as a real wizard: progress bar with `aria-current="step"` + "Step X of 4", an in-progress resume banner, Back / Continue / Skip, focus moves to the step heading on each change, Personal Setup (name / protected sleep / weekday capacity / starting mode / priorities) autosaving to the durable row, Connect Systems showing **real** Academics / Fitness / Money / Obsidian / AI status with per-optional-system "Connect / Set up / Skip", a Review screen listing the **actual** choices, and "Launch PBOS" that persists completion **before** navigating to Today. Scoped axe on the wizard = 0 critical/serious. |
| **First-launch routing** | DONE + browser-verified | `AppGate.tsx` reworked: the app renders only once BOTH the splash animation has finished AND durable state has loaded — the wrong route is never flashed. The first-boot decision is captured once (a ref) before `markFirstBootSeen` flips it, so the route stays stable through the splash. `e2e/setup.spec.ts` proves first-run (splash → Welcome → … → Today, survives a hard reload with no onboarding reappearance) and resume (interrupt at Connect Systems → hard reload → resumes there, earlier input intact). The approved splash visuals/timing are untouched. |
| **Connect Systems — real integrations** | DONE + browser-verified | Obsidian status = the canonical `useObsidian().hubState` (connected / not-set-up / offline) with a "Connect" link to the Notes Hub and a "Skip" that records the choice; AI status = the canonical `useAICoach().aiAvailability` (the built-in deterministic provider is genuinely "ready"; a disabled/not-configured provider shows truthfully) with a "Set up" link to the permissions screen. Optional systems can be skipped; none block launch. No fake "Connected" cards. |
| **Regression** | CLEAN | Provider tree re-ordered in `App.tsx` (Onboarding moved below AI Coach so it can read real system status). No functional change to Performance / Academics / Study / Focus / Mastery / Knowledge / Obsidian / Development / Fitness / Routines / Language / Money / Planning / Calendar / Today / Quick Capture / Analytics / AI Coach / Search. `useSettings().mode` (Normal Study) and `useSettings().appearance.reducedMotion` (splash) unchanged. Three older e2e specs' `APP_READY` boot regex extended to also match the new onboarding page (the `uppercase` class was removed from one onboarding label so a case-sensitive `/Performance Buddy OS/` matches). Core invariants hold: Settings configures canonical domains; Onboarding does not duplicate them; temporary override ≠ baseline; mode override ≠ baseline; AI cannot silently change protected config (no Apply adapter writes Settings — verified in Batch 6); optional-integration-not-configured ≠ error. Migrations v1–v8 unchanged. |
| Suite | vitest **78→80 files / 618→639** (+21: settings engine +7 / store.dom 6, onboarding engine +3 / store.dom 6, minus deleted seed churn); `cargo test --lib` 91→**96** (+5 `setup.rs`); Playwright **40→45** (+5 `e2e/setup.spec.ts`: first-run, resume, effective-config precedence, reset-onboarding-keeps-data, wizard a11y); `npm run build` ✓; `npm run lint` 0 errors; `cargo build` + `--release` ✓; `rustfmt` clean for `setup.rs`/`db.rs`/`lib.rs`; `cargo clippy` only the pre-existing `performance.rs` `usize→usize` warning. |
| **Native WebdriverIO / Tauri E2E** | BLOCKED — EXTERNAL TOOLCHAIN | Unchanged `@wdio/tauri-service@1.3.0` / WebView2 151.x issue; not investigated (per brief §45). Batch 7 native scenarios join the written-but-unrunnable set; rechecked at Batch 10. |

---

## Master Batch 8 — Resilience + general revision/audit store

**Status: COMPLETE — PASS. non-native evidence green (2026-09-01).** Continuation-run
completion of the partial started earlier the same day. Committed as
`feat(resilience): complete V1 resilience and revision history`.
The V1 Day-17 resilience contract is now real in the UI: every high-value async
surface shows a restrained LOADING state distinct from the honest EMPTY state;
a failed persist keeps the draft and offers a safe retry; an AI, Obsidian, or
per-route failure stays local and the deterministic core keeps working;
UNKNOWN / PARTIAL / STALE read truthfully, never as a fake zero. And the general
cross-domain **append-only revision/audit store** (P1-20's non-AI slice) exists,
is wired into the meaningful canonical mutations, and survives a reload.
Native Tauri desktop E2E remains **BLOCKED — EXTERNAL TOOLCHAIN** (rechecked at Batch 10).

| Area | State | Evidence |
|---|---|---|
| **Migration v10** | DONE | `db.rs` `CURRENT_SCHEMA_VERSION 9 → 10` — `revision_events` (append-only: `id` PK, `domain`, `entity_type`, `entity_id`, `operation`, `source`, `summary`, small `metadata` JSON, `created_at`) + 3 indexes. v1–v9 bodies **untouched** (only the version constant + a new forward-only tuple). `cargo test` `migrations_are_idempotent` covers v1→v10 on a fresh DB. |
| **`revision.rs` — Rust data-access** | DONE | `revision_append` (INSERT OR IGNORE — a retried/duplicate id is a no-op, never an overwrite), `revision_load` (newest-first, optional `domain`/`entityType`/`entityId` filter, `limit` capped at 1000), `revision_reset_for_test` (`#[cfg(debug_assertions)]` + `DbError::Forbidden` in release). **No update-one / delete-one command exists** (append-only by shape, brief §24). Registered in `lib.rs`. `cargo test --lib` **96 → 101** (+5: append+reload newest-first; append idempotent on id + first write immutable; filter by domain/entity; limit cap; `revision_events` carries no derived-truth column). `rustfmt --check` clean; `clippy` clean (only the pre-existing `performance.rs` `usize→usize`). |
| **`src/domains/revision/` — TS domain** | DONE | `types.ts` / `ids.ts` / `repo.ts` (`RevisionRepo` = Sqlite + Local, append-only interface — **no update/remove method**) / `recorder.ts` (module-singleton `recordRevision()` — fire-and-forget, **never throws / never rejects into the caller**, StrictMode-safe one-shot delay cache, in-memory feed + subscriber notify; a failed durable append is logged in dev and dropped) / `store.tsx` (`RevisionProvider` + `useRevision()` — durable load + live-merge, `eventsFor(query)`, `refresh()`; no dependency on any other store). |
| **Wired into canonical mutations** | DONE | `recordRevision` called from performance `createGoal` / `transitionGoal` / `createAction` / `setActionStatus`; routine `checkInRoutine`; knowledge `addEvidence`; settings `setBaseConfig` / `setMode`; planning `applyProposal` (re-plan); intelligence `apply` (AI-applied path → `source: "ai-applied"`, mapped to the right domain, **separate from `ai_decision_events` which is untouched**). `RevisionProvider` is the outermost provider in `App.tsx`. |
| **LOADING ≠ EMPTY** | DONE | New shared `src/components/StateViews.tsx` — `LoadingState` (`role="status"`), `ErrorState` (`role="alert"` + optional Retry), `PartialDataNotice` (`role="status"`), `StaleNotice`, `SaveErrorBanner` — restrained, reduced-motion-safe (`motion-safe:animate-pulse`), each with the right ARIA role. Adopted with a `!loaded` guard (and `loadError` → `ErrorState` where the store exposes it) on: Today, Planner, Calendar, Analytics Overview / Weekly / Monthly / Patterns, AI Coach, Settings, SGPA/CGPA, Money Overview / Transactions / Budget & Savings, Recovery, Daily Check-In, and the 6 domain overviews (Academics / Knowledge / Development / Fitness / Routines / Reading & Language) + Goals / Systems / Goal Detail / System Detail switched from inline "Loading…" text to the shared component. Notes Hub already distinguishes `hubState === "loading"` and `actionError` (left as-is — reuse over rebuild). |
| **SAVE FAILURE ≠ LOST DRAFT** | DONE (relational stores) | `testControls.assertRepoWritable()` added to the `performance` and `money` `LocalRepo` write paths (dev/test-only failure injection, tree-shaken from `npm run build` — 0 occurrences of any `__PBOS_DEV__` identifier in `dist/`). `resilience/contract.dom.test.tsx` + `e2e/resilience.spec.ts` B prove: a forced persist failure → optimistic value kept + "Save Failed" surfaced + the draft is NOT cleared; a retry after recovery persists exactly once and survives a hard reload. The Batch-0 `usePersistedState`/`mirrorWrite` path (settings) already covered by `usePersistedState.dom.test.tsx`. |
| **RETRY** | DONE | `SaveIndicator` retry (existing) for `saveState`; `ErrorState onRetry` on load-failure pages runs `window.location.reload()` (a read — no duplicate mutation); Notes Hub `refresh()` re-runs the idempotent scan; AI `apply` idempotency unchanged (Batch 6). No retry added to irreversible operations. |
| **OFFLINE-FIRST** | DONE (verified) | `ConnectivityBanner` (existing, `navigator.onLine` + events) is informational only, never blocking. `e2e/resilience.spec.ts` C proves the deterministic core (Today, Analytics) is fully usable with the AI path down. No new network dependency or cloud fallback introduced anywhere in the diff. |
| **AI FAILURE LOCALITY** | DONE (verified) | `intelligence/store.dom.test.tsx` (FakeProvider modes) + the new "AI-disabled: generate fails honestly, deterministic surfaces keep working" + `e2e/resilience.spec.ts` C. Batch 6 AI-authority architecture untouched. |
| **OBSIDIAN FAILURE LOCALITY** | DONE (verified) | `testControls.assertObsidianScanOk()` in the browser adapter's `scan()` (dev/test-only). `e2e/resilience.spec.ts` D: a forced vault scan error shows on the Notes Hub **only**; the Knowledge Topic still opens and its mastery/evidence are intact. `not-connected` / `missing` / `empty` / `error` states pre-exist from Batch 5. |
| **PARTIAL / UNKNOWN ≠ ZERO / STALE** | DONE (verified) | Reading book with no known total → honest "Total pages not tracked… This is not 0%" (`e2e/resilience.spec.ts` E + existing `language.dom.test.tsx`). Analytics `comparePeriods` → `insufficient` not 0% (Batch 6). Knowledge mastery / Fitness readiness / CGPA-with-no-grades → explicit unknown, not 0 (Batch 2/4/6 engines + tests). `StaleNotice` available for the one real stale case (Obsidian index vs moved file, Batch 5). No ordinary persisted data is mislabelled "stale". |
| **ERROR BOUNDARIES** | DONE (verified) | `RouteErrorBoundary` is per-route real (`componentDidCatch`), AppShell (sidebar/topbar) stays mounted around it, "Try Again" recovery + "your data is safe" copy. `e2e/resilience.spec.ts` asserts no "something went wrong" leaks on Today/Analytics during an AI outage. `/capture-inbox` is a real route since Batch 3 (the audit §17 raw-dev-error item is already closed). |
| **REACT TESTS** | DONE | vitest **639 → 660** (+21: `revision/` 13, `components/StateViews.dom.test.tsx` 6, `resilience/contract.dom.test.tsx` 2 — loading≠empty via injected delay + failed-save-keeps-draft-then-retry-persists, `intelligence/store.dom.test.tsx` +1 AI-applied revision event with the AI decision trail staying separate; minus 1 count reconciliation). 6 pre-existing `*.dom.test.tsx` helpers gained a one-line `await screen.findAllByRole(...)` to wait past the new LOADING state. |
| **PLAYWRIGHT** | DONE | full suite **45 → 52** (+7 `e2e/resilience.spec.ts`: A loading≠empty via a reload-surviving injected delay, B failed-save-keeps-draft + retry-persists-once, C AI-disabled → Today/Analytics usable, D Obsidian scan error stays local → Knowledge Topic still opens, E unknown-pages book ≠ 0%, F canonical mutation → revision event survives reload, + scoped axe on the LOADING surface = 0 critical/serious). All failure injection is dev/e2e-only (`window.__PBOS_DEV__.*` + seeded sessionStorage); no production toggle in the UI. |
| **ACCESSIBILITY** | DONE | `StateViews.dom.test.tsx` asserts `role="status"` (polite) for loading/partial/stale, `role="alert"` for error/save-failure, keyboard-reachable Retry/Refresh, and Loading ≠ Alert. `e2e/resilience.spec.ts` scoped axe (`.include("main")`) on the live LOADING surface = 0 critical/serious. (Whole-shell axe still has pre-existing contrast debt — that is Batch 9.) |
| **FULL GAUNTLET** | GREEN | `npm test` **660/660** (83 files) · `npm run lint` exit 0 (warnings-only, unchanged `only-export-components` class) · `npm run build` ✓ · full Playwright **52/52** · `cargo test --lib` **101/101** · `cargo build` ✓ · `cargo build --release` ✓ · `rustfmt --check` clean for `revision.rs`/`db.rs`/`lib.rs` · `cargo clippy` only the pre-existing `performance.rs` `usize→usize`. |
| **Native WebdriverIO / Tauri E2E** | BLOCKED — EXTERNAL TOOLCHAIN | Unchanged `@wdio/tauri-service@1.3.0` / WebView2 151.x issue; not investigated (per brief §20). Batch 8 native scenarios join the written-but-unrunnable set; rechecked at Batch 10. |

**Audit findings closed:** **P1-9** (loading / partial / stale states) → **CLOSED** — the Day-17 distinctions are now realised in the UI, not just the engine, with RTL + Playwright + scoped-axe evidence. **P1-20** (append-only revision / audit event store) → **CLOSED** — the AI slice landed in Batch 6 (`ai_decision_events`); the **general cross-domain slice** now exists (`revision_events`, migration v10), is append-only by shape (no update/delete-one), is wired into the meaningful mutations across 6 domains, records AI-applied changes as a distinct `source` without merging the AI trail, and survives a reload (`e2e/resilience.spec.ts` F). **P1-22** (accessibility) → still PARTIAL — the new resilience surfaces are covered; a global a11y pass + the command-search contrast item remain Batch 9/10.

**Post-Batch-8 completion score:** Overall **91%** · Functional **96%** · Visual/UX **79%**.

---

## Master Batch 9 — Global visual normalization + App Shell completion

**Status: PASS — non-native evidence green (2026-09-01). Committed as `style(ui): normalize V1 visual system and app shell`.**
Two runs: the foundation tier (fonts, tokens, shell regions, global focus +
reduced motion) then this continuation (shared-primitive normalization, Button
adoption, form/table normalization, per-screen typography, contrast/token
sweep, text-overflow guards, responsiveness + representative axe sweeps). The
whole existing PBOS V1 UI now sits on one coherent, WCAG-AA visual system with
no workflow regressions. Native Tauri desktop E2E remains **BLOCKED — EXTERNAL
TOOLCHAIN** (rechecked at Batch 10).

| Area | State | Evidence |
|---|---|---|
| **Fonts — genuinely loaded** | CLOSED | `public/fonts/*.woff2` — self-hosted, subsetted (latin/latin-ext) **variable** woff2 for Inter / Space Grotesk / JetBrains Mono (all SIL OFL 1.1; `public/fonts/README.md` attributes them). No font-package dependency, no machine-local path, no runtime network fetch (Tauri bundle). `src/tokens/fonts.css`: `@font-face` with `font-display: swap`, weight-axis range, per-subset `unicode-range`. `e2e/visual-system.spec.ts` asserts `document.fonts.check()` true for all three **and** `getComputedStyle` → body = `Inter Variable`, `h1` = `Space Grotesk Variable`. Bundled font payload ≈ 269 KB (4 files). |
| **Fonts — semantic usage** | CLOSED | `@layer base`: `body { font-family: var(--font-sans) }` (Inter → all UI/body/nav/forms/tables/AI text); `h1..h6 { font-family: var(--font-display) }` (Space Grotesk → every heading). Focus timer uses `.t-timer` (JetBrains Mono, tabular-nums) explicitly — verified in `FocusPage`. Splash wordmark switched from the missing non-variable `'Space Grotesk'` inline style to `font-display`. |
| **Color tokens reconciled** | CLOSED | `src/tokens/tokens.css` — foundation tier = locked hexes; semantic tier kept by name so every screen keeps resolving. `text.muted` `#6F7883 → #7c8490` (canonical value fails AA on `bg-base` **and** `bg-surface-raised`; 07.03 records the hexes as PROVISIONAL pending contrast review — this is that review). 83 `text-text-disabled` misuses on readable caption/hint text across 30 files → `text-text-muted` (token-semantics + contrast fix; `text.disabled` now only on genuinely disabled controls). |
| **Hardcoded style drift** | CLOSED (documented remainder) | Repo sweep: **zero** `#hex` in `.tsx` except `main.tsx`'s pre-React loading screen (canonical values, runs before CSS — legitimate). **zero** `rgb()/rgba()` in components. `text-white`/`bg-black`/`zinc|slate|cyan|gray-`/arbitrary-blue: **zero**. Two intentional literals kept: `SplashScreen` `bg-black` (approved cinematic backdrop, §41) and `CommandPalette` `bg-black/60` (standard modal scrim). Inline styles: 6 × `style={{width:`${pct}%`}}` (legitimate dynamic progress widths, §26) + the splash font fix. |
| **Typography scale** | CLOSED | Canonical `--text-xs..4xl` size/line-height pairs; `.t-hero/.t-h1/.t-h2/.t-h3/.t-card-title/.t-body(-lg)/.t-small/.t-label/.t-nav/.t-metric-lg/.t-metric-md/.t-timer/.t-mono`. Applied: **52** page-section headers across 50 files `text-xl font-semibold → .t-h2`; StatCard → `.t-label` + `.t-metric-md`; EmptyState → `.t-card-title` + `.t-small`; RecommendationCard / ProposalCard / FocusPage / SgpaCgpaPage headers + captions → `.t-*`. Global heading display-font via base layer. |
| **Spacing / radii / borders / shadow** | CLOSED (tokens + shell) | Canonical spacing steps (4→64) + shell metrics; radii `--radius-sm..2xl` = 6/8/12/16/20; `--shadow-raised/modal/float` + `--ring-focus`. `Card` gains `emphasis` (primary/secondary/tertiary surface weight) + `padding` (16/20/24) props, defaults unchanged. No stray `rounded-3xl` / arbitrary `shadow-[...]` found in the sweep. |
| **Focus states** | CLOSED | One global `@layer base` rule: `:where(a, button, input, select, textarea, summary, [tabindex], [role=button|tab|option|menuitem]):focus-visible { outline: 2px solid var(--color-accent-primary); outline-offset: 2px }`. `FormFields` dropped its per-field ring in favour of the global one. `e2e/visual-system.spec.ts` asserts the command-search button focuses. |
| **Buttons — inventory** | CLOSED (per §35) | Raw `<button>` (non-test) **~276 → 145**. `<Button>` usages **103**; `FormActions` (submit+cancel footer) in **16** files; `buttonClass()` on **8** styled `<Link>`s. The remaining 145 raw are intentional specialized controls: segmented mode/coverage pickers (Normal Study, System Detail), calendar blocks + week nav (Calendar Week), list-row status toggles, inline `underline` text-link actions, icon/close affordances, the success-tinted Focus "Finish" control. PASS = ordinary reusable actions consistently use `Button`/`FormActions`; raw controls are deliberate. |
| **Buttons — migration** | DONE | `src/components/Button.tsx` (primary/secondary/ghost/danger/icon · sm-32/md-40/lg-44 · loading/disabled · radius 8 · `.t-button` sizing · global focus ring) + `buttonClass()` helper for links. Adopted via: `FormActions` codemod across 14 `*Form.tsx`; a conservative exact-className codemod (83 buttons / 36 files) preserving `onClick`/`disabled`/`type`; hand pass on `EmptyState`, `StateViews`, `RecommendationCard`, `ProposalCard`, `FocusPage`, `TopBar`, `RouteErrorBoundary`. |
| **Forms** | DONE | `FormFields.tsx` normalized: 40px input/select height (`h-10`), `min-h-[80px]` textarea, `.t-label` labels, `.t-small` hint/error, canonical `space-y-1.5`, global focus ring. Propagates to every form (Goal/Action/System, Course/Topic/Assessment, Knowledge Topic/Source, Project/Skill, Plan, Routine, Language Path/Book, Transaction). Submit/cancel footer unified via `FormActions` (canonical control height + spacing). No form semantics changed — 215 form/domain DOM tests still green. |
| **Cards / panels** | DONE | `Card` emphasis tiers give screens Day-18 hierarchy (PRIMARY/SECONDARY/TERTIARY) via surface weight instead of identical outlined boxes; title → `.t-label`. StatCard → canonical metric type. |
| **Tables** | DONE | `SgpaCgpaPage` course-projection table normalized: `.t-label` uppercase headers, `py-3` (~44px) rows, `.t-body`. Transaction / budget lists are `flex` rows with `border-b` dividers + `tabular-nums` — already table-shaped, left as rows (not cardified). Real `<table>` count in the app = 4; all use muted headers + subtle dividers + honest empty rows. |
| **Badges** | DONE | `Badge` gains `info` tone + optional `dot` (non-colour cue) + `.t-label` sizing. Tone set stays semantic (neutral/success/warning/danger/info/ai) — no per-domain decorative colours. Text label always carries the meaning. |
| **App Shell — notification/status region** | CLOSED | `src/shell/ShellStatusRegion.tsx` in the TopBar — save state (loading / not-saved / fallback-store / saved-locally, from real `usePersistenceStatus`) + offline pill. No fabricated feed; no OS-notification claim. |
| **App Shell — context rail** | CLOSED (structure + reference) | `src/shell/ContextRail.tsx` — a route opts in via `handle.contextRail`; `AppShell` renders a 300px `lg:`-and-up right rail only when provided, nothing otherwise. Reference: `/focus` → `FocusContextRail` (today's finished focus minutes + active session method/target — never the running timer, never a mastery number). `e2e/visual-system.spec.ts` asserts present on `/focus`, absent on Today + Academics. Adoption elsewhere is a product-design decision — documented, not invented. |
| **App Shell — sidebar / topbar** | CLOSED (expanded) | Sidebar → canonical 248px, `.t-nav` items, 36px rows, `accent-soft` active fill, canonical section labels, factual footer. TopBar rebuilt: `.t-h3` title, real command-search `<button>` (opens palette via `pbos:open-command-palette`), status region, AI entry now a real `NavLink` to `/ai-coach`. **Collapsed-72 icon rail: DEFERRED — product-design decision** (needs a decided per-destination icon set; none exists). Expanded nav is fully usable, so this does not block PASS. |
| **Command search contrast (§21)** | CLOSED | Focusable `<button>`, `text-secondary`/`text-muted` on `bg-surface` (AA), hover + global focus ring, `aria-label` + `aria-keyshortcuts`. Command-palette hint text `text-disabled → text-muted`. `e2e/resilience.spec.ts` scoped-axe + `e2e/a11y-sweep.spec.ts` (Command Palette overlay) green. |
| **Reduced motion** | CLOSED | `@media (prefers-reduced-motion: reduce)` **and** `:root[data-reduced-motion="true"]` collapse animation/transition/scroll-behavior globally. `AppGate` mirrors `useSettings().appearance.reducedMotion` onto `<html>`, reaching route transitions / menus / modals / AI-card entry / command palette / context rail / splash. |
| **Text overflow (§43)** | CLOSED | Global `@layer base`: `overflow-wrap: break-word` on `h1..h6` + `p/li/dd/dt/td/th`. Detail-page headers rendering user names use `truncate` + `min-w-0`. `e2e/visual-system.spec.ts` fills a 120-char goal title + a 250-char rationale at 1024px and asserts zero horizontal overflow. |
| **Responsiveness (§42)** | CLOSED | `e2e/visual-system.spec.ts` — no horizontal overflow at **1024 / 1280 / 1440 / 1600 / 1920** across Today / Planner / Calendar / Focus / Academics / Money / Analytics / AI Coach Workspace / Settings (9 screens × 5 widths). Context rail hides below `lg`; sidebar + main never collide. |
| **Accessibility (§45/55) — P1-22** | CLOSED | `e2e/a11y-sweep.spec.ts` boots once and runs axe (`wcag2a`+`wcag2aa`, scoped to `#root`) on **18 routes** — Today, Goals, Planner, Calendar, Academics, Normal Study, SGPA/CGPA, Focus, Knowledge, Notes Hub, Development, Fitness, Routine, Language, Money, Analytics, AI Coach, Settings — plus the Command Palette overlay and the Onboarding wizard. **0 critical / 0 serious**, and 0 medium/minor logged. Combined with the pre-existing scoped-form axe (×N) and first-paint axe. |
| **Keyboard (§47)** | DONE | Global focus ring visible on every interactive role; command palette arrow/enter/esc unchanged; form submit/cancel are real `<button>`s via `FormActions`; sidebar `NavLink`s, TopBar search button, and the AI link are all keyboard-reachable (asserted by the a11y sweep's role queries + the focus assertion). |
| **New Playwright** | DONE | `e2e/visual-system.spec.ts` (fonts loaded+applied, command search, context-rail presence, long-text wrap, 9×5 responsiveness) + `e2e/a11y-sweep.spec.ts` (18-route axe + palette + onboarding). |
| **Regression** | CLEAN | No domain logic / engine / store / **Rust** / **migration** change. Schema stays **v10**. `App.tsx` provider tree untouched. 87 frontend files changed (net −72 lines). vitest 660/660, Playwright green, cargo 101/101 — no functional coverage lost. |
| **FULL GAUNTLET** | GREEN | `npm test` **660/660** (83 files) · `npm run lint` exit 0 (warnings-only: unchanged `only-export-components` class + one pre-existing `set-state-in-effect`) · `npm run build` ✓ (css 34.8→~36 KB gz ~7.5 KB; JS unchanged; fonts fingerprinted into `dist/fonts/`) · full Playwright **GREEN** · `cargo test --lib` **101/101** · `cargo build` ✓ · `cargo build --release` ✓. |
| **Native WebdriverIO / Tauri E2E** | BLOCKED — EXTERNAL TOOLCHAIN | Unchanged `@wdio/tauri-service@1.3.0` / WebView2 151.x issue; not investigated (per brief). Rechecked at Batch 10. |

**Audit findings closed:** **Typography / fonts** → **CLOSED**. **Visual token drift** → **CLOSED** (canonical palette reconciled; `text-text-disabled` misuse + muted-contrast fixed; hardcoded-style sweep clean with a documented, intentional 2-literal remainder). **App Shell missing regions** → **CLOSED** (notification/status + context-rail structure operational; collapsed sidebar explicitly deferred as a product-design decision). **Command-search contrast** → **CLOSED**. **Reduced motion** → **CLOSED**. **Global accessibility visual pass (P1-22)** → **CLOSED** — representative 18-route axe sweep, 0 critical/serious.

**Product decisions still open (do NOT block PASS — brief §44):**
- **P1-11 navigation IA** — still `PRODUCT DECISION REQUIRED`. Not invented. Navigation is fully usable.
- **Collapsed sidebar (72px icon rail)** — `PRODUCT DESIGN DECISION` (needs a decided per-destination icon set). Expanded nav is complete.
- **Additional context rails beyond `/focus`** — per-screen content is a design decision; the reusable structure ships, adoption is deliberate.

**Post-Batch-9 completion score:** Overall **95%** · Functional **96%** · Visual/UX **91%**.
Visual/UX 83→91: whole-app typography/token/contrast normalization, shared-primitive + Button/Form adoption, the closed A11y P1-22 sweep, verified responsiveness, and text-overflow guards. Functional unchanged (no workflow was blocked or altered). Overall 92→95 on the visual + a11y closures.

---

## Master Batch 10 — Final forensic QA + stabilization + release-candidate decision

**Status: PASS — RELEASE CANDIDATE = YES (2026-09-01).** Independent-QA-style pass
over the committed V1 (`a7d6871`). No new product features, no architecture change,
no visual redesign. The full automated gauntlet was re-run from clean; every decided
workflow is exercised by fresh RTL + real-Chrome E2E evidence; persistence, the
migration chain, security, and accessibility were re-reviewed; the native toolchain
blocker was re-checked within a bounded window; and the release-build path was
assessed. One P2 hygiene defect was found and fixed. Committed as
`chore(release): stabilize Performance Buddy OS V1`.

### Baseline

Started at `a7d6871` · branch `main` · working tree clean · synced with `origin/main`. Schema **v10**.

### Full gauntlet — fresh, this batch

| Step | Result |
|---|---|
| `npm test` (Vitest) | **660 / 660** (83 files) — unchanged; no `.skip` / `.only` / disabled suite anywhere |
| `npm run lint` (oxlint) | exit **0** — warnings-only (the unchanged `only-export-components` class + 1 pre-existing `set-state-in-effect` in `MasteryCheckPage`) |
| `npm run build` (`tsc -b && vite build`) | **PASS** — bundle warning (>500 KB JS chunk) is pre-existing and documented in Batch 9 |
| full Playwright (`npm run test:e2e`) | **63 / 63** (2.4 min) — includes the 18-route + Command Palette + Onboarding axe sweep |
| `cargo test --lib` | **101 / 101** — 0 ignored, 0 filtered; `migrations_are_idempotent` (v1→v10) green |
| `cargo build` | **PASS** |
| `cargo build --release` | **PASS** — `target/release/app.exe` 13 MB |
| `cargo fmt --check` | pre-existing churn in `main.rs` + `performance.rs` **only** (present since before Batch 0; every batch has left it untouched by decision — not a Batch-10 regression) |
| `cargo clippy --lib` | 1 warning — the pre-existing `performance.rs:457` `usize→usize` unnecessary cast, documented in every batch since Batch 2 |
| axe (via Playwright) | **0 critical / 0 serious**, 0 medium/minor logged |

No baseline regression. Test totals held exactly (660 / 63 / 101); nothing was deleted, skipped, or weakened to stay green.

### Workflow / persistence QA (Sections A–S of the brief)

Every decided V1 workflow in the brief is covered by fresh, re-run **React-Testing-Library DOM tests + real-Chrome Playwright** evidence (the same bundle Tauri loads). Representative proofs re-verified green this batch: first-run splash→Welcome→…→Today + hard-reload persistence; interrupted-onboarding resume at the saved step; reset-onboarding keeps domain data (`e2e/setup.spec.ts`); Goal↔System↔Action relationship + reload (`performance.spec.ts`); ACTION→PLANNER→CALENDAR→TODAY with completion + history survival, conflict ≠ capacity, locked/manual block survives regenerate (`planning.spec.ts`); Quick Capture ×4 types + ambiguous-survives-reload, no duplicate on re-confirm (`planning.spec.ts` + `capture/store.dom.test.tsx`); Professor Coverage ≠ Personal Study ≠ Mastery, Focus-time ≠ mastery, one idempotent Knowledge-evidence handoff (`study.spec.ts`); Obsidian connect→index→link with mastery unchanged, stale-file keeps Knowledge (`obsidian.spec.ts`); base plan byte-unchanged by an actual workout (`fitness.spec.ts`); one canonical routine log/day (`routine.spec.ts`); UNKNOWN reading total ≠ 0% (`language.spec.ts` + `resilience.spec.ts` E); savings-transfer ≠ expense, planned ≠ actual, budget from real expenses only (`money.spec.ts`); Analytics honest insufficient-comparison, no universal score (`intelligence.spec.ts`); AI Accept ≠ Apply, deterministic validation, Reject = zero mutation, Modify applies the edited value, invalid Apply blocked, valid Apply → one canonical `generated` block + persisted decision trail (`intelligence.spec.ts` + `applyAdapters.test.ts`); effective-config precedence 90→135→150→135→60→90 with base never mutated (`setup.spec.ts`); LOADING ≠ EMPTY, failed-save keeps draft + retry persists once, AI/Obsidian/route failure stays local, revision event survives reload (`resilience.spec.ts` A–F).

Interactive hand-QA beyond the automated Chrome suite was **not** performed in-session (two Chrome browsers connected — selection needed; not worth interrupting for coverage the automated Chrome + RTL suites already provide). The packaged desktop binary cannot be driven — see native blocker.

### Migration / persistence (Section T)

Schema **v10**; `db.rs` last touched in Batch 8 (`6de9c24`) — Batch 9 and Batch 10 add no migration. `MIGRATIONS` is a forward-only `&[(i64,&str)]`; bodies v1–v9 are `CREATE TABLE IF NOT EXISTS` / additive `ALTER … ADD COLUMN` only and are unchanged since introduction (git-verified). Every one of the 101 Rust tests runs the full v1→v10 chain on a fresh in-memory DB; `migrations_are_idempotent` re-runs it and asserts the final version. `localstorage_migration_is_idempotent_validated_and_non_destructive` covers legacy-data safety (`ON CONFLICT DO NOTHING`, legacy keys never deleted). No dedicated "pre-v10 DB fixture with rows → migrate → rows survive" test exists; acceptable — the migrations are additive-only by construction and `schema_migrations` gates re-application. **No v11 created.**

### Security (Sections U / 58 / 82–85)

CLEAN on every checked axis:
- **Secret scan** — no API keys / bearer tokens / private URLs / personal filesystem paths / credentials in tracked `src/` or `src-tauri/src/`. Test fixtures are clearly fake.
- **AI credentials** — `ai_config` has no key column (Rust `config_never_has_a_key_column`, banned set `api_key`/`key`/`secret`/`token`/`credential`); `RemoteOpenAIProvider` reads `VITE_PBOS_AI_API_KEY` at call time only, never stored / logged / prompted; `ai_status` exposes only a boolean presence flag; no key in localStorage / SQLite / logs.
- **Generic AI mutation** — no `applyJsonPatch` / `writeAnyTable` / `executeArbitraryCommand` / `eval` / `new Function` / AI-provided SQL / AI-provided Tauri command anywhere in non-test code. `APPLY_ADAPTERS` is a hard 4-kind allowlist (`create-action`, `schedule-block`, `set-knowledge-review`, `adjust-routine-cadence`); `getAdapter("apply-json-patch")` → `null`.
- **Obsidian containment** — `safe_join` rejects `..`, absolute, rooted, and (via `canonicalize` + `starts_with` re-check) symlink/junction escape; preview bounded 64 KiB; **read-only** — no write command is registered in `invoke_handler` (the one `fs::write` in the file is inside `#[cfg(test)]`).
- **Debug commands** — every `*_reset_for_test` is `#[cfg(debug_assertions)]` + returns `DbError::Forbidden` in release. Production bundle: **0** occurrences of any `__PBOS_DEV__` identifier, `console.log`, or `TODO`/`FIXME` in shipping source; dev failure-injection is tree-shaken.

### Accessibility (Section S)

Batch 9's `e2e/a11y-sweep.spec.ts` (18 routes + Command Palette + Onboarding, `wcag2a`+`wcag2aa`) + every domain's scoped-form axe + first-paint axe re-ran green this batch: **0 critical / 0 serious**, 0 medium/minor. Global focus-visible ring on every interactive role; command palette + forms + sidebar keyboard-reachable (asserted by the sweep's role queries + focus assertion).

### Native Tauri E2E (Sections V / 86–91) — BLOCKED, EXTERNAL

Bounded recheck (2026-09-01): `npm run test:e2e:tauri` — **0 passed / 1 failed** in 2:11. All 9 native scenarios fail with `"Tauri plugin not available"`; the WebDriver session establishes against `webview2 151.0.4129.107` and the debug `app.exe` builds + launches, but `browser.execute` cannot reach the plugin bridge through the CDP surface WebView2 151 exposes. Upstream: `@wdio/tauri-service` **1.3.0** and `@wdio/tauri-plugin` **1.3.0** are the latest published (npm registry; nothing newer since 2026-08-03); `tauri-driver` **v2.0.6**; `msedgedriver` **151.0.4129.107** == WebView2 Runtime **151.0.4129.107** (exact match — not a driver/runtime mismatch). No upstream fix and no reversible local change exists (the only levers are a system-wide WebView2 downgrade — out of scope — or an upstream release). Per brief §88: nothing changed, no package churn. This is **not** an internal V1 blocker — Rust tests, browser workflows, RTL, and the Batch-0 native-renderer capability proof are all green, and the blocker is demonstrably external.

### Release build (Section X)

- **Rust release binary** — `cargo build --release` ✓ → `src-tauri/target/release/app.exe`, 13 MB. Debug `app.exe` (18 MB) builds + launches (the native recheck rebuilds and runs it).
- **Installer bundle** — **not produced.** The repo defines no Tauri production *bundle* script (only `tauri:build:debug` = a plain `cargo build`), and `tauri build` cannot succeed as configured: `tauri.conf.json` still carries the scaffold `identifier: "com.tauri.dev"` (Tauri refuses to bundle this), `productName: "app"`, and `security.csp: null`. Producing a signed installer is an **operator action** (see below), not an internal acceptance item (brief §97).
- **Signing** — not configured. `SIGNING: OPERATOR ACTION REQUIRED`.

### Bugs

**Found (1):**
- **P2 — dev-only "Simulate relaunch" affordance shipped in the production Onboarding UI.** `OnboardingPage.tsx` rendered `<button onClick={simulateRelaunch}>Simulate relaunch (replays the startup gate in place)</button>` unconditionally. It is safe (a pure React re-render of the startup gate — no data mutation, no failure injection) but it is developer-facing language and behaviour, and a vestige of the pre-persistence onboarding the audit's P1-6 called out by name ("'Simulate Relaunch' exists precisely because of this"). Now that onboarding is durable (Batch 7), it should not be in a release build.

**Fixed (1):**
- Gated the affordance behind `import.meta.env.DEV`. Root cause: never guarded when introduced. Verification: `npm run build` then `grep "Simulate relaunch (replays" dist/assets/*.js` → **0** (the string and its `<div>` are gone from the production bundle; only the inert `simulateRelaunch` store function name remains in minified JS). No unit test added — the guard is a build-time concern and jsdom runs with `DEV=true`; the 27 existing onboarding tests still pass. No Playwright/Vitest test referenced the control (grep-verified), so nothing regressed.

**Remaining (0 internal).** Operator/config items are listed under "Operator actions" — none block the RC.

### Audit reconciliation (Section Z)

All 6 P0 findings **CLOSED** (P0-6 "zero UI/workflow test coverage" is closed by the now-standing 660 Vitest incl. RTL DOM + 63 Playwright + 101 cargo). P1 status (superseding the stale rows in the P1 table below): P1-13 (fonts), P1-14 (App Shell regions), P1-22 (a11y) → **CLOSED in Batch 9**, re-verified green here. **P1-11 (navigation IA)** → remains `PRODUCT DECISION REQUIRED` — navigation is fully usable; not invented. No open internal P0/P1/P2 after the fix above.

### Approved V1 limitations (documented, not defects)

No OS-level notification scheduling (honest preferences only) · one dark theme · Quick Note does not silently write Obsidian · Obsidian V1 = read-only metadata/index boundary, no note bodies in SQLite · no RAG/vector/semantic search · Past Papers / adaptive testing deferred (`docs/17`) · no Fitness→Knowledge evidence handoff (out of V1 scope) · AI real-provider network smoke is an operator/manual step (suite uses the deterministic FakeProvider, makes no paid call) · no mobile companion · no cloud sync/multi-device · collapsed-72px sidebar icon rail deferred (needs a decided icon set) · context rail adopted on `/focus` only (further adoption is a design decision).

### Operator actions before a public V1.0 release

1. Set a real bundle `identifier` (≠ `com.tauri.dev`) and `productName` in `src-tauri/tauri.conf.json`; add a real `security.csp`.
2. Configure code signing; run the Tauri production bundler to produce the installer(s); verify the installed app launches against a clean profile.
3. Optional: one manual smoke of `RemoteOpenAIProvider` against a real endpoint with a locally-supplied key (never committed).
4. Optional: manual native desktop smoke (the automated native path stays externally blocked until an upstream `@wdio/tauri-service` release supports WebView2 151+).
5. Begin 7–14 day real-world dogfooding; fix usage-driven issues; schedule the later manual UI-refinement phase.

### Score

| | Previous (Batch 9) | Batch 10 final |
|---|---|---|
| Overall | 95% | **96%** |
| Functional | 96% | **97%** |
| Visual/UX | 91% | **92%** |

Movement is deliberately small: Batch 10 added **no features** — it converted "tests were green previously" into *freshly re-verified* green across every suite, closed the last testing-coverage P0 (P0-6) with standing evidence, completed the security + migration + a11y + release-path review the RC decision needs, and removed one dev affordance from the production UI. The remaining gap to 100% is entirely the documented **external** native-automation blocker and **operator** packaging/signing steps — neither an internal V1 defect.

### Release-candidate decision

**PERFORMANCE BUDDY OS V1 — RELEASE CANDIDATE: YES.** No internal blocker, no open internal P0/P1. Critical workflows, persistence, and the v1→v10 migration chain all verified. Full Vitest / Playwright / cargo green; production web build + Rust release build green; security review clean; accessibility 0 critical/serious; no secret exposure; no destructive migration. The one remaining automation gap (native desktop E2E) is demonstrably external, and all known V1 limitations are documented. RC ≠ public release — packaging, signing, and publishing are the operator steps above.

---

## Master Batch 5 — Knowledge / Obsidian boundary + shared Evidence

**Status: COMPLETE — non-native evidence green (2026-08-31). Committed as `feat(knowledge): complete V1 Obsidian and evidence integration`.**
The V1 Obsidian slice is operable: a user connects a real local vault, PBOS scans it read-only and indexes **metadata only** (no note bodies in SQLite), the Notes Hub shows the honest state (not-connected / empty / indexed / missing / error), notes link to Knowledge Topics as governed references that survive a rescan and a missing file, and Open/Reveal run through a safe path-checked Rust command (browser mode says so honestly). A shared Evidence presentation component is adopted on Knowledge Topic Detail, Development Skill Detail and the Mastery Result. Development↔Knowledge is a single reference plus an explicit, idempotent evidence handoff. Native Tauri desktop E2E remains **BLOCKED — EXTERNAL TOOLCHAIN** (rechecked at Batch 10).

| Area | State | Evidence |
|---|---|---|
| Migration **v7** | DONE | `db.rs` `CURRENT_SCHEMA_VERSION = 7` — `obsidian_config` (single-row), `obsidian_notes` (metadata-only: path/title/mtime marker/size/`exists_on_disk`; **no content/body column** — Rust `no_content_column_exists_on_the_note_index`), `knowledge_topic_note_links` (governed reference keyed by `relative_path`, `UNIQUE(topic_id, relative_path)`, CASCADE on the Topic side only) + `ALTER TABLE development_skills ADD COLUMN knowledge_topic_id` (FK → knowledge_topics, SET NULL) + `ALTER TABLE development_skill_evidence ADD COLUMN knowledge_evidence_id` (FK → knowledge_evidence, SET NULL). v1–v6 bodies **untouched**; `migrations_are_idempotent` covers v1→v7. |
| **Obsidian module** — `obsidian.rs` + Rust tests | DONE | Least-privilege filesystem (docs 16.04): `canonical_vault` validates dir + canonicalises; `safe_join` rejects `..`, absolute, rooted, and — via canonicalise + containment re-check — symlink/junction escapes. `scan_vault` is a manual bounded-depth walk, `*.md`/`*.markdown` only, skips `.git`/`.obsidian`/`.trash`/`node_modules`/…; title = first `# ` heading else filename stem; index is a tombstone→revive→purge reconcile that keeps a missing-but-linked note as visible `stale`. Preview read is on-demand + 64 KiB-bounded. `obsidian_open_note`/`obsidian_reveal_note` path-check then hand off to the OS handler (never called from tests). Commands: `obsidian_load/connect_vault/disconnect_vault/scan/link_note/unlink_note/read_note/open_note/reveal_note/reset_for_test`, registered in `lib.rs`. `cargo test` +10: reject missing dir / non-dir; nested-md-only scan ignoring dot-dirs; **no content column**; safe_join traversal/absolute/escape/missing; a link survives rescan + a deleted linked file goes stale (topic + evidence intact); deleting a topic cascades its links but never the vault; disconnect drops the disposable index but keeps links + files; bounded scoped preview; reset. |
| **Notes Hub** — `knowledge/NotesHubPage.tsx` rewritten | DONE + browser-verified | Was a placeholder ("Obsidian not connected"). Now: `not-connected` (vault path form + read-only/metadata-only copy), `missing` (offline vault, scanning paused, links kept), `empty`, `indexed` (connected header + last-scan summary, deterministic title/filename/path search, per-note Open/Reveal + link-to-Topic, disconnect), `error` (notes-index problem is explicitly *not* a Knowledge problem). Linked-notes panel resolves each link `in vault` / `missing / stale` / `not indexed`. Browser mode drives a clearly-labelled dev-vault **adapter** (`obsidian/repo.ts`) — no fake native claims (brief §29). |
| **Obsidian store / engine / repo** | DONE | `obsidian/{types,engine,repo,store}.ts(x)` + `ObsidianProvider` wired into `App.tsx` inside Knowledge. `engine.ts` (pure): `noteMatchRank` exact→prefix→contains, `searchNotes` (rank then path A→Z), `resolveLink` → `ok`/`stale`/`unindexed`, `matchNoteByReference`. `store.tsx`: `hubState` derivation, `connect/disconnect/refresh/linkNote/unlinkNote/openNote/revealNote/readNote`, `linksForTopic`, `resolveLinkState`. Tests: `engine.test.ts` (5), `repo.test.ts` (7 — adapter connect/scan/link/stale/purge/disconnect/preview), `store.dom.test.tsx` (5 — states, link never touches mastery, stale keeps Knowledge, disconnect keeps links). |
| **Topic ↔ Note on Topic Detail** | DONE + browser-verified | Knowledge Topic Detail gains a "Linked Notes — Obsidian" card: link an indexed note, unlink, Open, and an explicit `missing / stale` / `not indexed` badge; honest "no vault connected" state. `e2e/obsidian.spec.ts`: connect → index → search → link → Topic shows the note → **mastery is still "unknown"**; an externally-removed linked file goes `missing / stale` and the Topic + its Evidence are untouched; link + stale state + evidence survive a reload. |
| **Knowledge review** | DONE | `engine.nextReviewDateFor(state, from)` (deterministic: strong 14d / developing 7d / learning 3d / new 2d) + `store.markReviewed(topicId)` — bumps last-studied + next-review only, **never evidence** (Master Handoff §5). "Mark reviewed" button on Topic Detail with the "updates the review schedule only — never mastery" note. Review queue already surfaced on the Knowledge overview. |
| **Shared Evidence component** — `components/EvidenceList.tsx` | DONE | One presentational component (no state, no aggregate/"universal evidence score"). Each caller maps its own canonical records to `EvidenceView[]`. Adopted on Knowledge Topic Detail (Evidence list), Development Skill Detail (Evidence list + a per-row cross-domain action slot), and available for the Mastery Result. |
| **Development ↔ Knowledge** — `development.rs` + store/repo/UI | DONE + browser-verified | ONE reference: `development_skills.knowledge_topic_id` (dangling-safe — `dev_skill_link_knowledge` stores a non-existent topic as NULL; the browser path resolves a dangling id to null on read to match the FK). Skill Detail: link/unlink a Knowledge concept, read-only Knowledge mastery shown. Explicit **evidence handoff**: `sendEvidenceToKnowledge(evidenceId)` → deterministic provenance weight (`independent` 1/1, `ai-assisted-reviewed` 0.7/1, `ai-assisted` **refused** — "AI writing code is not you understanding it", §14) → canonical `useKnowledge().addEvidence` (`type: "practice"`) → `dev_skill_evidence_link_knowledge` sets `knowledge_evidence_id` **set-once** (returns the existing id, deletes a losing race's duplicate). `cargo test` +2 (skill references one concept + survives its deletion + upsert never writes the column; evidence handoff set-once + SET NULL on evidence delete + unknown-id safe). `devKnowledge.dom.test.tsx` (4): link doesn't raise capability; handoff = exactly one Knowledge row, idempotent; unreviewed AI refused; no-link refused (nothing invented). |
| **Global Search** | DONE | `search/store.tsx` indexes Obsidian note **metadata** (`entityType: "note"`, route `/knowledge/notes`) — title/filename/path only, rebuilt fresh from `useObsidian().notes` each call, never a stored copy. No note bodies, no semantic search. |
| **Book note references** | DONE | `language/BookDetailPage.tsx`: when a vault is connected, a `book.noteRef` is reconciled against the index — `matched in vault` + Open, or an explicit `unresolved` state; honest "connect a vault to resolve it" when none is connected. `book.noteRef` free text is unchanged and never rewritten. |
| **Quick Capture note / note creation** | NOT DONE (by decision) | Brief §20/§21: no silent Markdown writes. `docs/16.01` `CORE` is read-only by default; PBOS-created notes are an explicit later feature. The honest Capture "note" boundary from Batch 3 is left intact; no `obsidian_write_*` command exists. |
| **Fitness Evidence** | UNTOUCHED (by decision) | Brief §19 default — no Decision Spec requires Fitness↔Evidence integration in Batch 5. Workout completion still never becomes Knowledge mastery. |
| **Security review** | DONE | Path traversal, absolute/rooted paths, and symlink/junction escape are rejected by `safe_join` (canonicalise + `starts_with` containment) — covered by `safe_join_rejects_traversal_and_absolute_and_escape` and `read_note_preview_is_bounded_and_scoped`. Indexing never writes to the vault (no write path exists). The Rust commands take only a vault path / relative path / ids; every relative path is resolved against the stored canonical root. `obsidian_reset_for_test` is `#[cfg(debug_assertions)]` + `DbError::Forbidden` in release. |
| **Regression** | CLEAN | No functional change to Performance / Academics / Study / Focus / Mastery / Planning / Calendar / Today / Quick Capture / Fitness / Routines / Language / Money / Analytics / Onboarding. Development gains the Knowledge reference + handoff only (capability axes, evidence score, project links unchanged — `development.dom.test.tsx` still green). Core invariants hold: Note ≠ Topic ≠ Evidence; Source ≠ Evidence; Review ≠ Mastery; Skill ≠ Knowledge Topic; Project progress ≠ Skill capability; Knowledge Evidence remains the mastery truth. Migrations v1–v6 unchanged. |
| Suite | vitest **71→75 files / 550→571** (+21: obsidian engine 5 / repo 7 / store.dom 5, devKnowledge.dom 4, minus churn); `cargo test --lib` 71→**83** (+12: obsidian 10, development 2); Playwright **32→35** (+3 `e2e/obsidian.spec.ts`: connect→index→search→link (mastery unchanged), stale-file (Knowledge survives) + reload, scoped Notes-Hub axe = 0 critical/serious); `npm run build` ✓; `npm run lint` 0 errors; `cargo build` + `--release` ✓; `rustfmt` clean for `obsidian.rs`/`development.rs`/`db.rs`/`lib.rs`; `cargo clippy` only the pre-existing `performance.rs` `usize→usize` warning. |
| **Native WebdriverIO / Tauri E2E** | BLOCKED — EXTERNAL TOOLCHAIN | Unchanged `@wdio/tauri-service@1.3.0` / WebView2 151.x issue; not investigated (per brief §35). Batch 5 native scenarios join the written-but-unrunnable set; rechecked at Batch 10. |

---

## Master Batch 6 — Analytics / Reviews + AI decision loop

**Status: COMPLETE — non-native evidence green (2026-08-31). Committed as `feat(intelligence): complete V1 analytics and AI decision loop`.**
The last major functional link of the operating loop is closed: ANALYTICS → REVIEW → AI COACH → RECOMMENDATION → USER DECISION → DETERMINISTIC VALIDATION → SAFE CANONICAL APPLY → RE-PLAN runs end to end (`e2e/intelligence.spec.ts`). The AI never holds a write credential; every canonical change goes through an allowlisted Apply adapter that validates first. Decision history is durable and append-only. Native Tauri desktop E2E remains **BLOCKED — EXTERNAL TOOLCHAIN** (rechecked at Batch 10).

| Area | State | Evidence |
|---|---|---|
| Migration **v8** | DONE | `db.rs` `CURRENT_SCHEMA_VERSION 7 → 8` — `ai_config` (provider id / model / base URL / enabled — **no key column**, Rust `config_never_has_a_key_column`), `ai_permissions` (durable per-domain level), `ai_recommendations` (one canonical model — kind / domain / title / rationale / evidence JSON / confidence / source / proposed+current params JSON / status / validation JSON / applied_result JSON / decided_at / applied_at), `ai_decision_events` (**append-only** — CASCADE on the recommendation, no update/delete-one path), `analytics_reviews` (**immutable** — `INSERT OR IGNORE`, one `snapshot` blob). v1–v7 bodies **untouched**; `migrations_are_idempotent` covers v1→v8. |
| **AI provider abstraction** — `domains/ai/` | DONE | `AIProvider` interface + `AIRequest` / `AIResponse` / `ContextManifest` / `ProposedRecommendation` (docs 24.12). `FakeProvider` — deterministic, the only provider wired by default and in every test, with a `mode` that drives all failure classes (timeout / network / auth / rate-limit / malformed / empty). `RemoteOpenAIProvider` — the real boundary: builds an OpenAI-compatible chat/completions request, 30 s `AbortController` timeout, normalises 401/403→auth, 429→rate-limit, non-2xx→unknown, JSON parse failure→malformed, empty content→empty; the API key is read from `VITE_PBOS_AI_API_KEY` **at call time only**, never stored/logged/prompted. `NullProvider` (disabled / not-configured) never makes a call. `makeAIProvider(config, credentialsPresent)` picks by config; switching providers never changes authority or the approval flow. Rust `ai_status` reports **only a boolean** for `PBOS_AI_API_KEY` presence. Tests: `ai/provider.test.ts` (18), `ai/context.test.ts` (5). |
| **Per-domain permissions + context minimisation** | DONE | `ai/context.ts`: `buildContextManifest(requested, permissions, facts)` = requested **∩** (read | read-recommend); every other known domain is listed explicitly as excluded; facts are `[Domain] bullet` strings only — **no raw JSON, no marks, no note bodies, no transaction amounts**. Money defaults to **no-access** (docs 30.05). `parseProposals` rejects any proposal whose `kind` is not allowlisted, has no title, or targets a domain without Read+Recommend — with a reason, never silently dropped. `store.dom.test.tsx` proves a Money-targeted proposal is refused and Money facts never enter the manifest; `e2e/intelligence.spec.ts` shows the Included/Excluded preview. |
| **Analytics engine + Overview** | DONE + browser-verified | `analytics/engine.ts` +`isoInWindow` / `startOfWeekIso` (UTC-safe) / `monthBounds` / `completionRate` (null on no data — unknown ≠ zero) / `comparePeriods` (**`insufficient`** when the prior window is missing, never 0%) / `deriveDataSufficiency` / `buildMonthlyReview` (deep-copy, immutable) / `derivePatterns` (deterministic Pearson over routine-completion series, honest `INSUFFICIENT EVIDENCE` below the min sample, worded as association-not-cause). The Overview reads real per-domain snapshots (each keeps its own unit — no universal score), this-week routine completion + data sufficiency, and the pattern list. `analytics/engine.test.ts` +15, `analytics/store.dom.test.tsx` (4). |
| **Weekly / Monthly / Patterns screens** | DONE + browser-verified | New routes `/analytics/weekly`, `/analytics/monthly`, `/analytics/patterns` (closes 3 of P1-2's remaining screens + P1-19). Weekly Review: deterministic **facts first**, then user notes, then an optional "ask the coach to interpret" — the three are never blended (docs 22.04). Monthly Review: calendar-month scope, this-vs-prior comparison that shows "insufficient comparison — no complete prior month yet" rather than a fabricated delta (docs 22.05). Patterns: confidence tier + correlation wording + insufficient state. Reviews are durable via `analytics/repo.ts` → `analytics_review_append` (immutable) and reload across a remount. |
| **AI Coach — Overview / Workspace / Permissions** | DONE + browser-verified | `AICoachPage` reworked: 5-state availability (ready / disabled / not-configured / unavailable) with calm copy, deterministic insights from Analytics that work with AI off, the recommendation queue, combined-capacity impact, and an append-only decision history showing the event chain (`proposed → accepted → applied`). `AICoachWorkspacePage` (new `/ai-coach/workspace`) — task-scoped: pick domains, see the human-readable context preview, ask; the conversation is transient (docs 26.02) and any proposal becomes a durable recommendation. `AICoachPermissionsPage` (new `/ai-coach/permissions`) — the per-domain matrix (durable), the provider config (no secret), and an explicit "what the coach can / can never access" (closes §36 + P1-2's AI Workspace). |
| **Recommendation model + decision history** | DONE + browser-verified | ONE canonical `Recommendation` (not one table per domain). States `proposed → accepted / modified / rejected → applied / apply-failed`. `intelligence/repo.ts` persists recommendations (upsert; status is current state) and decision events (append-only; the browser LocalRepo also refuses an event for a missing recommendation). `store.dom.test.tsx`: a rejected recommendation stays in history; the full trail + applied state survive a remount. |
| **Accept / Modify / Reject + deterministic validation + safe Apply** | DONE + browser-verified | `intelligence/applyAdapters.ts` — the allowlist: exactly four V1 kinds (`create-action`, `schedule-block`, `set-knowledge-review`, `adjust-routine-cadence`). Each adapter `validate()`s deterministically (Phase-23-style reason codes: `NO_SYSTEM`, `CONFLICT`, `CAPACITY_EXCEEDED`, `INVALID_TIME`, `NO_TOPIC`, `INVALID_DATE`, `NO_ROUTINE`, `INVALID_CADENCE`) and, only if valid, calls **exactly one** canonical store method (`createAction` / `createBlock` / `updateReviewState` / `updateRoutine`). There is **no** `applyJsonPatch` / `writeTable` / `runCommand` / AI-provided SQL. `getAdapter("apply-json-patch")` → `null`. `schedule-block` runs the canonical planning `checkFit` (conflict + capacity) — the re-planning gate. `applyAdapters.test.ts` (11), `store.dom.test.tsx`: Accept+Apply makes one real Planning block (`source: "generated"`); Reject = zero mutation; Modify applies the **edited** value (30, not the AI's 60); an impossible duration → `apply-failed` + `INVALID_TIME`, **no block created**. `e2e/intelligence.spec.ts` proves the same through the real UI incl. the Planner showing the applied block and the decision trail surviving a hard reload. |
| **Audit trail** | DONE (V1 slice) | `ai_decision_events` is the append-only decision/apply trail (docs 30.16, P1-20's AI slice) — `proposed` / `accepted` / `modified` / `rejected` / `applied` / `apply-failed` with a small `detail` snapshot and a timestamp, CASCADE-linked to its recommendation, no update path. A general cross-domain revision store is still later work. |
| **Real provider smoke** | DEFERRED (documented) | Per brief §35 the batch PASSes on provider-boundary correctness + the deterministic test adapter. No secret is required by the suite; `RemoteOpenAIProvider` fails **closed** to `not-configured` without `VITE_PBOS_AI_API_KEY` (tested). A one-off manual smoke against a real endpoint is left to the operator; the key is never printed or committed. |
| **Money / Academic / Knowledge / Fitness safety** | DONE | No Apply adapter can execute a transaction, edit a mark, change Professor Coverage, add Knowledge evidence, or rewrite a Base Training Plan — the four kinds only create an Action, schedule a Planning Block, set a review date, or change a routine cadence. Money is no-access by default and its facts never enter context. AI explanation ≠ Evidence (no evidence-writing adapter exists). |
| **Regression** | CLEAN | Provider tree re-ordered in `App.tsx` (Analytics + AI Coach moved below Planning/Settings so the Apply adapters can reach the canonical stores) — no functional change to Performance / Academics / Study / Focus / Mastery / Knowledge / Obsidian / Development / Fitness / Routines / Language / Money / Planning / Calendar / Today / Quick Capture / Search / Onboarding. `TodayPage` still reads `useAICoach().visibleRecommendations` unchanged. Core invariants hold: AI suggestion ≠ canonical state; Recommendation ≠ mutation; Accept ≠ Apply (validation between them); AI explanation ≠ Evidence; AI cannot bypass a deterministic engine; manual/locked plan blocks are untouched by a re-plan (the adapter only *adds* a `generated` block); Money ≠ performance score. Migrations v1–v7 unchanged. |
| Suite | vitest **75→76 files / 571→618** (+47: `ai/provider` 18, `ai/context` 5, `intelligence/engine` rewritten 8, `intelligence/applyAdapters` 11, `intelligence/store.dom` 6, `analytics/engine` +15, `analytics/store.dom` 4, minus the deleted seed test); `cargo test --lib` 83→**91** (+8 `ai.rs`); Playwright **35→40** (+5 `e2e/intelligence.spec.ts`: analytics/weekly/monthly/patterns honesty, generate→Accept→Apply→Planner→reload, Reject/Modify/Invalid, AI-disabled, scoped a11y); `npm run build` ✓; `npm run lint` 0 errors; `cargo build` + `--release` ✓; `rustfmt`/`clippy` clean for `ai.rs` (only the pre-existing `performance.rs` `usize→usize` clippy warning). No paid AI call anywhere in the suite. |
| **Native WebdriverIO / Tauri E2E** | BLOCKED — EXTERNAL TOOLCHAIN | Unchanged `@wdio/tauri-service@1.3.0` / WebView2 151.x issue; not investigated (per brief §56). Batch 6 native scenarios join the written-but-unrunnable set; rechecked at Batch 10. |

---

## P0 Findings → Batch assignment

| ID | Finding (audit §20) | Batch | Status | Verified when |
|---|---|---|---|---|
| P0-1 | No create/edit UI for any configuration entity | 1 (Goal/System/Action — **DONE**), 2 (all others) | **IMPLEMENTED (browser-verified)** — every V1 configuration entity across all 8 domains now has create + edit + delete in-app, persisted to relational SQLite, with no seed/code edit: Goal/System/Action (Batch 1), Course/Semester/Topic/Assessment/Attempt + Knowledge Topic/Source/Evidence (Batch 2A), Project/Skill/Milestone/Evidence + Project↔Skill link (Development), Training Plan/Planned Session/Workout Session/Recovery check-in (Fitness), Routine/RoutineLog + System link (Routines), Language Path/Unit/Session + Book/Reading Session (Reading & Language), Transaction/PlannedExpense/Budget/SavingsGoal (Money). Verified by RTL interaction tests + Playwright per domain + Rust/SQLite read-back for Batch 1/2A. Native desktop read-back for Batch 2B/2 is written but BLOCKED (external toolchain — see native row). Remaining config entities Block/Capacity are Batch 3. |
| P0-2 | Planner / Calendar / Plan Builder do not exist; planning has no mutations | 3 | **CLOSED (Batch 3, browser-verified)** — Planner Overview + Plan Builder + Calendar Week all operable; `planning/store` has full block CRUD + capacity + Generate→Review→Apply; locked/manual blocks survive regeneration (`PlannerPage.dom` + `e2e/planning.spec.ts`). | `e2e/planning.spec.ts` "Conflict ≠ Capacity, and a locked block survives a regenerate"; `PlannerPage.dom.test.tsx` |
| P0-3 | Quick Capture broken (unreachable, non-persisted inbox; `/capture-inbox` 404) | 2 → 3 | **CLOSED (Batch 3, browser-verified)** — durable `capture_inbox` (SQLite + LocalRepo); `/capture-inbox` route added; palette inline Confirm / Change-type / Keep-in-Inbox; confirmed captures delegate to the canonical Action / Transaction / Routine-log engine with no duplicate writes; ambiguous capture survives reload. | `e2e/planning.spec.ts` "Quick Capture — Action, Expense, Routine, and an ambiguous capture that survives reload"; `capture/store.dom.test.tsx`, `capture/repo.test.ts` |
| P0-4 | Core operating loop cannot be completed | spans 1–4, 6 | **CLOSED (Batch 6, browser-verified)** — every stage now runs end to end. Batch 1: GOAL / GOAL→SYSTEM / SYSTEM→ACTION. Batch 3: ACTION→PLANNER→CALENDAR→TODAY. Batch 4: TOPIC→STUDY→FOCUS→MASTERY→EVIDENCE→KNOWLEDGE. **Batch 6** closes the last link: ANALYTICS→REVIEW→AI COACH→RECOMMENDATION→USER DECISION→DETERMINISTIC VALIDATION→SAFE CANONICAL APPLY→RE-PLAN (`e2e/intelligence.spec.ts` "AI apply loop": generate → Accept → Apply → a real `generated` Planning block → Planner shows it → decision trail + state persist a reload). No direct AI write occurred at any point. |
| P0-5 | No real AI provider anywhere | 6 | **CLOSED (Batch 6)** — `domains/ai/`: `AIProvider` interface + `FakeProvider` (deterministic, default + all tests), `RemoteOpenAIProvider` (real OpenAI-compatible boundary — full failure-class normalisation, 30 s timeout, key read from `VITE_PBOS_AI_API_KEY` at call time only), `NullProvider` (disabled / not-configured). `makeAIProvider(config, credentialsPresent)` selects by config without changing authority. Rust `ai_status` exposes only a boolean for `PBOS_AI_API_KEY` presence. No secret is stored, logged, prompted, or committed; the suite makes no paid call. `ai/provider.test.ts` (18). |
| P0-6 | Zero UI / workflow / integration test coverage | 0 (pattern), then every batch, full in 10 | **CLOSED (Batch 10)** — standing suite: **660** Vitest (incl. RTL DOM interaction tests) + **63** Playwright (real Chrome, every decided workflow + 18-route axe sweep) + **101** cargo. All re-run green from clean in Batch 10; no `.skip`/`.only`/disabled suite. | Batch 10 fresh gauntlet |

---

## P1 Findings → Batch assignment (audit §21)

| # | Finding | Batch | Status |
|---|---|---|---|
| P1-1 | Goal Detail is a shell | 1 | **IMPLEMENTED** — now a real management surface: Edit Goal, meta (type/priority/deadline/progress), Why-this-goal, Linked Systems with Manage (link existing / create-for-goal / unlink), actions-from-linked-systems view, safe lifecycle transitions, delete. Milestones + Evidence panel + AI Insight remain deferred (docs 11.05 milestones = later; Evidence infra = P1-17). |
| P1-2 | 19 decided screens have no route | 2–6 (per domain) | **CLOSED (Batch 6)** — the remaining three landed: `/analytics/weekly` (dedicated Weekly Review + persistence), `/analytics/monthly` (Monthly Review), `/analytics/patterns` (Patterns & Insights), plus `/ai-coach/workspace` and `/ai-coach/permissions`. Every decided V1 screen now has a real route. |
| P1-3 | Obsidian V1 integration entirely absent | 5 | **CLOSED (Batch 5, browser-verified)** — real vault connect + validate (least-privilege, canonical-root path safety, symlink-escape rejected), read-only recursive `*.md` scan, metadata-only index (no note bodies in SQLite), operational Notes Hub with all honest states, Topic↔Note governed links that survive rescan/missing-file, Open/Reveal via safe Rust command, on-demand bounded preview. `obsidian.rs` (10 Rust tests) + `obsidian/*` TS (17 tests) + `e2e/obsidian.spec.ts` (3). Semantic/RAG/embeddings correctly still out of scope (V2+). |
| P1-4 | No workout session logging (Actual side of Base≠Prescription≠Actual) | 2 | **IMPLEMENTED (browser-verified)** — Fitness Active Workout records an independent `WorkoutSession` (own row; `plan_id` / `planned_session_id` SET NULL FKs). Editing the Base Plan never rewrites it and completing it never rewrites the plan — proven by Rust `base_plan_is_untouched_by_recording_an_actual_session`, `repo.test.ts`, `fitness.dom.test.tsx` and `e2e/fitness.spec.ts` (actual reps `15,14,11` vs prescription `3 × 15`, base byte-for-byte unchanged). |
| P1-5 | AI recommendation "Apply" step missing; no persisted decision history/audit | 6 | **CLOSED (Batch 6)** — `intelligence/applyAdapters.ts` is the allowlisted Apply pipeline (4 kinds, deterministic validation with Phase-23 reason codes, each calling exactly one canonical store method; no generic mutation). Decision history + an append-only `ai_decision_events` trail are durable (`ai.rs` migration v8; `store.dom.test.tsx` + `e2e/intelligence.spec.ts` reload proofs). Invalid Apply is blocked with no mutation. |
| P1-6 | Onboarding not persisted (not resumable across restart) | 7 | **CLOSED (Batch 7, browser-verified)** — one durable `onboarding_state` row (migration v9) + `OnboardingRepo`/store. The current step, `firstBootExperienceSeen`, entered Personal Setup and system choices all persist; a mid-flow reload resumes at the saved step with input intact (`onboarding/store.dom.test.tsx` remount + `e2e/setup.spec.ts` "resume"). `AppGate` reads the durable first-boot flag and routes deterministically (first-install → Welcome; normal → Today; interrupted → resume; migrated existing user → Today). Completion is idempotent; `resetOnboarding` clears only the workflow row (Rust `reset_onboarding_clears_only_onboarding_never_settings_or_domain_data` + `e2e/setup.spec.ts`). |
| P1-7 | Capture Inbox not persisted (compounds P0-3) | 2 → 3 | **CLOSED (Batch 3)** — `capture_inbox` table (migration v5) + `capture.rs` + relational repo/store; the inbox reloads across a full restart (`capture/store.dom.test.tsx` remount assertion, `e2e/planning.spec.ts` reload assertion). |
| P1-8 | AI decision / recommendation history not persisted | 6 | **CLOSED (Batch 6)** — `ai_recommendations` (durable, status is current state) + `ai_decision_events` (append-only, CASCADE-linked, no update path). Reload proofs in `intelligence/store.dom.test.tsx` and `e2e/intelligence.spec.ts` ("proposed → accepted → applied" survives a hard reload). |
| P1-9 | No loading / partial / stale states in the UI | 8 | **CLOSED (Batch 8, browser-verified)** — shared `components/StateViews.tsx` (`LoadingState`/`ErrorState`/`PartialDataNotice`/`StaleNotice`/`SaveErrorBanner`, correct ARIA roles, reduced-motion-safe) adopted with a `!loaded` guard on every high-value async surface (Today, Planner, Calendar, Analytics ×4, AI Coach, Settings, SGPA, Money ×3, Recovery, Daily Check-In, all 6 domain overviews + Goals/Systems/detail). A load ERROR renders `ErrorState` with a safe reload-retry. `resilience/contract.dom.test.tsx` (injected delay → LOADING visible, EMPTY only after loaded) + `e2e/resilience.spec.ts` A + scoped axe. UNKNOWN≠ZERO / PARTIAL≠FAILED / STALE≠CURRENT verified truthful (`e2e/resilience.spec.ts` E + Batch 2/4/6 engine tests). |
| P1-10 | `/calendar` route is wrong (sidebar "Calendar" → conflict checker) | 3 | **CLOSED (Batch 3)** — `/calendar` now renders the decided **Calendar Week** screen (a view over canonical Planning Blocks); the conflict/capacity + Plan Builder surface moved to `/planner`. Both are sidebar "core" items. |
| P1-11 | Nav structure conflict unresolved (`UI ↔ ARCHITECTURE REVIEW REQUIRED`; Systems missing from sidebar) | 3 (needs product decision — see below) | BLOCKED (product decision) — **partially advanced:** Planner + Calendar are now distinct sidebar items; the "is Systems top-level / one canonical sidebar" decision is still open (below). |
| P1-12 | `setSimulateStorageFailure` in production Routines UI | 0 | IMPLEMENTED (0.7) |
| P1-13 | Typography incomplete — Space Grotesk & JetBrains Mono not loaded | 9 | **CLOSED (Batch 9)** — all three faces self-hosted (`public/fonts/*.woff2`, no external fetch), `document.fonts.check()` + computed `font-family` asserted in `e2e/visual-system.spec.ts`; Focus timer uses `.t-timer` JetBrains Mono. Re-verified green in Batch 10. |
| P1-14 | App Shell missing regions (context rail, notifications, companion entry) | 9 | **CLOSED (Batch 9)** — `ShellStatusRegion` (save/offline state) + `ContextRail` structure (`/focus` reference adoption) + real command-search + AI `NavLink`. Collapsed-72px icon rail explicitly deferred as a product-design decision. |
| P1-15 | Today missing collapse model / priority bands / open-day state | 3 | **CLOSED (Batch 3)** — Today's Plan groups canonical blocks **NOW / NEXT / LATER / EARLIER** by current time; the Day-17 honest **open-day state** ("Your day is open", Open Planner / Open Calendar — never a shaming message) is used; linked blocks show the Action's live status + a Focus entry. `TodayPage.dom.test.tsx` + `e2e/planning.spec.ts`. |
| P1-16 | `performance` domain has zero tests | 1 | **VERIFIED** — `engine.test.ts` (24: validation, transitions, relationship resolution, health/insufficient-data, progress, attention), `legacyImport.test.ts` (9), `repo.test.ts` (7: CRUD, FK integrity, cascade, reorder, import idempotence), `performance.dom.test.tsx` (6: create/edit/link/add-action/status/validation-preserves-input, AI-proposal-never-auto-creates), plus `cargo test` (3: relational CRUD, FK rejection, import idempotence), Playwright + native E2E. |
| P1-17 | No shared Evidence infrastructure | 4–6 (introduce + adopt) | **CLOSED (Batch 5)** — Batch 4 established the one canonical `useKnowledge().addEvidence` path + set-once idempotency guard. **Batch 5** adds the shared presentational `components/EvidenceList.tsx` (no state, no universal score — each caller maps its canonical records), adopted on Knowledge Topic Detail, Development Skill Detail and the Mastery Result, and adds the explicit idempotent **Development→Knowledge** evidence handoff (`development_skill_evidence.knowledge_evidence_id`, set-once; raw unreviewed AI-assisted evidence is refused). Fitness evidence integration was confirmed out of Batch 5 scope. |
| P1-18 | Per-entity create/edit gaps (Course/attempt, Topic/Source, Project/Skill/Milestone, Budget/Savings, Book, Block, Capacity) | 2 → 3 | **CLOSED** — Batch 2 covered all configuration entities except Planner's; **Batch 3** adds Planning Block create/edit/move/lock/delete (Plan Builder + Calendar detail panel) and the Capacity editor (canonical `planning_capacity`), both persisted + RTL + Playwright covered. |
| P1-19 | Analytics Weekly Review not persisted; Monthly Review absent | 6 | **CLOSED (Batch 6)** — dedicated `/analytics/weekly` (facts-first, notes-separate, durable) and `/analytics/monthly` (calendar-month scope, this-vs-prior with an honest "insufficient comparison" state). Both persist via `analytics_review_append` (immutable `INSERT OR IGNORE`) and reload across a remount (`analytics/store.dom.test.tsx`). |
| P1-20 | No append-only revision / audit event store (`docs/32` data class 3) | 6 (with AI audit trail) or 8 | **CLOSED (Batch 8, browser-verified)** — the AI slice landed in Batch 6 (`ai_decision_events`). Batch 8 adds the **general cross-domain slice**: migration **v10** `revision_events` + `revision.rs` (`revision_append` INSERT OR IGNORE, `revision_load` filtered, `revision_reset_for_test` debug-only) — **append-only by shape, no update-one / delete-one command**. `src/domains/revision/` (`recorder` singleton — fire-and-forget, never throws into the caller; `RevisionProvider`/`useRevision`). Wired into meaningful mutations across 6 domains (Goal create/lifecycle, Action create/status, Routine check-in, Knowledge evidence, Settings base/mode, Planning re-plan, AI apply). An AI-applied canonical mutation records `source: "ai-applied"` **without merging the AI decision trail** (`intelligence/store.dom.test.tsx`). Reload-persistence proven in `e2e/resilience.spec.ts` F. Metadata is a small before/after, never a full entity snapshot (`revision/repo.test.ts` + Rust `revision_store_is_not_authoritative`). |
| P1-21 | Native desktop QA unautomatable (couldn't reach renderer) | 0 | VERIFIED (0.8 — `browser.tauri.execute` drives the real renderer) |
| P1-22 | No accessibility verification beyond first-paint axe on `/` | 10 (a11y pass), incremental per batch | **CLOSED (Batch 9, re-verified Batch 10)** — `e2e/a11y-sweep.spec.ts`: axe (`wcag2a`+`wcag2aa`) on 18 routes + Command Palette + Onboarding = **0 critical / 0 serious**, 0 medium/minor; plus every domain's scoped-form axe + first-paint axe. Global focus-visible ring on all interactive roles; keyboard reachability asserted. |

---

## P2 / P3 / V2+

P2 (audit §22, 15 items) — folded into Batch 9 (visual normalization) and the owning domain's batch. Not individually tracked until their batch starts.
P3 (audit §23) — Batch 9 / deferred to the post-V1 manual redesign phase.
V2+ (audit §24) — `DEFERRED V2+`, out of scope. Not tracked here.

---

## Open product decisions blocking specific findings

| Decision (audit §19) | Blocks | Needed for batch |
|---|---|---|
| Navigation IA — one canonical sidebar structure; is Systems top-level | P1-11 | 3 |
| Academic repeat-grade policy (`RESEARCH REQUIRED`) | accurate CGPA | 4 |
| Score→letter-grade scale | grade automation | 4 |
| AI provider for V1 (LOCAL vs approved CLOUD vs CORE-only ship) | P0-5 | 6 |
| `tokens.css` reconcile to locked palette + promote `docs/07.03` to APPROVED | P2 visual | 9 |
| Authoritative `Normal Study` reference; status of Mastery screens (under `/Review/`) | Batch 4 screens | 4 |
| Onboarding scope — 4 screens vs 1 page; minimum persisted baseline | P1-6 | 7 |

SQLite-for-V1 was an open decision in the audit; **resolved by this batch** — SQLite/Rust is now the implemented durable architecture, localStorage is dev-only fallback.

---

## Score movement log

| Date | Batch | Overall | Functional | Visual/UX | Note |
|---|---|---|---|---|---|
| — | pre-0 | 38% | 30% | 55% | audit baseline |
| — | 0 | 38% | 30% | 55% | **infrastructure only — headline % deliberately unchanged.** Durable persistence, migration, native E2E capability, and a UI test pattern do not by themselves complete any decided user workflow. |
| — | 1 | **45%** | **38%** | **57%** | Goal→System→Action spine now genuinely operable. Changed audited gaps: audit §3 problems A (read-only), B (dueling relationship arrays), C (health stored twice), D (seed masquerading as user data) — all resolved. §8 core loop: (create) GOAL, GOAL→SYSTEM, SYSTEM→ACTION BROKEN → PASS. §9 CRUD matrix: Goal C/U, System C/U, Action C/U + full status, Goal↔System link — all now ✓. §12: "No Goal Engine / No System Engine" ABSENT → PRESENT (relational model + `engine.ts`). §6 screens: Goal Builder MISSING→PASS, Goal Detail SHELL→operational, Systems Overview + System Detail PARTIAL→operational. Test coverage: a formerly-zero-test domain now has 46 tests across 6 layers. **Not moved:** Planner (Batch 3), 12 other config entities (Batch 2), AI apply (Batch 6), global visual normalization (Batch 9). |
| 2026-08-31 | 6 | **86%** | **93%** | **74%** | **Master Batch 6 complete — the operating loop is closed and the AI decision path is real.** ANALYTICS → REVIEW → AI COACH → RECOMMENDATION → USER DECISION → DETERMINISTIC VALIDATION → SAFE ALLOWLISTED APPLY → RE-PLAN runs end to end (`e2e/intelligence.spec.ts`). **Functional 88→93:** closes P0-4 (the last core-loop link — USER DECISION → RE-PLANNING now PASS), P0-5 (real provider abstraction: Fake + Remote-OpenAI-compatible + Null, no secret stored/committed), P1-5 + P1-8 (allowlisted Apply adapters with Phase-23-style validation + durable append-only decision history), P1-19 (Weekly persisted + Monthly added), P1-2 (the final 3 missing screens + AI Workspace/Permissions — every decided V1 screen now routes), and the AI slice of P1-20 (`ai_decision_events`). Per-domain permissions are durable, Money is no-access by default and its facts never enter context, and every canonical mutation goes through exactly one store method — no generic write API exists. Reject = zero mutation, Modify applies the edited value, invalid Apply is blocked with a reason code — all proven in RTL + Playwright. **Visual/UX 72→74:** five new operable surfaces (Weekly / Monthly / Patterns / AI Workspace / AI Permissions) plus a reworked AI Coach Overview and a shared `RecommendationCard`, on the locked tokens (scoped axe 0 critical/serious). No global normalization (Batch 9). **Overall 82→86 (not higher):** onboarding not persisted (Batch 7), the app-wide resilience/loading pass (Batch 8), global visual normalization + fonts + App Shell regions (Batch 9), and **native Tauri desktop verification stays BLOCKED** (external toolchain, rechecked at Batch 10); the real-provider network smoke is left as an operator step. Migration **v8** (`ai_config` / `ai_permissions` / `ai_recommendations` / `ai_decision_events` / `analytics_reviews`); v1–v7 untouched. Tests: vitest 571→**618** (+47), Playwright 35→**40** (+5), `cargo test` 83→**91** (+8). No paid AI call in the suite. |
| 2026-09-01 | 8 | **91%** | **96%** | **79%** | **Master Batch 8 complete — the Day-17 resilience contract is real in the UI, and the general revision/audit store exists.** LOADING ≠ EMPTY on every high-value async surface (shared `StateViews` with correct ARIA roles + `!loaded` guards); a failed persist keeps the draft and offers a safe retry; AI / Obsidian / per-route failures stay local and the deterministic core keeps working; UNKNOWN / PARTIAL / STALE read truthfully, never a fake zero. **Functional 95→96:** closes **P1-9** (loading/partial/stale states realised in the UI, not just the engine — RTL + Playwright + scoped axe) and **P1-20** (general cross-domain append-only `revision_events`, migration v10 — no update/delete-one, wired into 6 domains' meaningful mutations, AI-applied changes recorded as a distinct `source` without merging `ai_decision_events`, reload-persistent). Save-failure draft-retention proven for the relational stores via a dev/test-only injection (tree-shaken from `npm run build` — 0 `__PBOS_DEV__` identifiers in `dist/`). **Visual/UX 77→79:** consistent restrained loading/error/partial surfaces replace ad-hoc inline "nothing here" text across ~25 pages, on the locked tokens; scoped axe on the new surfaces = 0 critical/serious. No global normalization (Batch 9). **Overall 89→91 (not higher):** global visual normalization + fonts + App Shell regions + the command-search contrast item + a global a11y pass (Batch 9), full forensic QA + migration/security review + release-build + RC assessment (Batch 10), and **native Tauri desktop verification stays BLOCKED** (external toolchain, rechecked at Batch 10). Migration **v10** (`revision_events`); v1–v9 untouched, forward-only. Tests: vitest 639→**660** (+21), Playwright 45→**52** (+7), `cargo test` 96→**101** (+5); `cargo build --release` ✓. |
| 2026-09-01 | 9 | **95%** | **96%** | **91%** | **Master Batch 9 complete — one coherent WCAG-AA visual system, no workflow regression.** Fonts genuinely self-hosted + applied, canonical tokens + contrast fixes, global focus-visible + reduced-motion, `Button`/`FormActions`/`Card`/`Badge`/`Table` normalization, 52 headers → `.t-h2`, App Shell status region + context-rail structure, command-search contrast, text-overflow guards, responsiveness 1024–1920, 18-route axe sweep 0 critical/serious. Closes the fonts / token-drift / App-Shell-regions / command-search-contrast / reduced-motion / P1-22 audit items. **Visual/UX 79→91.** No engine/store/Rust/migration change (schema stays v10); vitest 660, Playwright green, cargo 101 — no coverage lost. P1-11 IA + collapsed-72px sidebar left as explicit product-design decisions. |
| 2026-09-01 | 10 | **96%** | **97%** | **92%** | **Master Batch 10 complete — RELEASE CANDIDATE = YES.** No new features. Fresh full gauntlet from clean: Vitest **660/660**, Playwright **63/63**, cargo **101/101**, `npm run build` ✓, `cargo build --release` ✓, axe **0 critical/0 serious**; no `.skip`/`.only`/disabled suite; test totals held exactly. Persistence, the v1→v10 migration chain (idempotent, additive-only, bodies unchanged), security (no secrets, no AI key column, no generic AI mutation — 4-kind allowlist, Obsidian read-only + containment, debug helpers `Forbidden` in release), and accessibility all re-reviewed clean. **1 P2 found + fixed:** the dev-only "Simulate relaunch" affordance was rendering unconditionally in the production Onboarding UI (a pre-persistence vestige, audit P1-6) — gated behind `import.meta.env.DEV`, verified stripped from `dist/`. Closes **P0-6** (standing 660+63+101 suite). **Native Tauri E2E stays BLOCKED — EXTERNAL TOOLCHAIN** (`@wdio/tauri-service@1.3.0` ↔ WebView2 151.x; 1.3.0 is latest published, exact driver/runtime match, no upstream fix) — demonstrably external, not an internal P0. **Release bundling** (real `identifier`/`productName`, code signing, CSP, installer) = operator action, not an internal acceptance item. Schema **v10** unchanged. One commit `chore(release): stabilize Performance Buddy OS V1`; not pushed. |
| 2026-08-31 | 7 | **89%** | **95%** | **77%** | **Master Batch 7 complete — the entry / configuration lifecycle is durable.** First launch → splash → Welcome → Personal Setup → Connect Systems → Review → Launch → Today; normal launch → splash → Today; an interrupted setup resumes at the saved step with input intact; a migrated existing user is never forced through first-run (`e2e/setup.spec.ts`). **Functional 93→95:** closes P1-6 (durable, resumable onboarding — one `onboarding_state` row, deterministic `AppGate` routing, idempotent completion, safe reset that never deletes domain data) and completes the Settings architecture (one canonical `settings_config` row replacing `usePersistedState`/`SEED_*`, a deterministic Base → Mode → Temporary → Effective engine with an explicit precedence trace; temporary > mode > base, non-destructive). Connect Systems reads real Obsidian + AI Coach status (no fake "connected" cards); Personal Setup writes into the canonical Settings store via one atomic commit, not an onboarding-owned copy. **Visual/UX 74→77:** a real onboarding wizard (progress semantics, `aria-current`, focus management, Back/Continue/Skip, honest system statuses, real Review summary) + a reworked multi-section Settings page on the locked tokens (scoped axe 0 critical/serious). No global normalization (Batch 9). **Overall 86→89 (not higher):** the app-wide resilience / loading / partial / stale pass + a general cross-domain revision store (Batch 8), global visual normalization + fonts + App Shell regions (Batch 9), and **native Tauri desktop verification stays BLOCKED** (external toolchain, rechecked at Batch 10). Notifications are honest preferences only (no OS scheduler yet); V1 ships a single dark theme. Migration **v9** (`onboarding_state` / `settings_config`); v1–v8 untouched. Tests: vitest 618→**639** (+21), Playwright 40→**45** (+5), `cargo test` 91→**96** (+5). |
| 2026-08-31 | 5 | **82%** | **88%** | **72%** | **Master Batch 5 complete — the Knowledge / Obsidian boundary is operable and the shared Evidence layer exists.** A real local vault connects and is scanned read-only into a **metadata-only** index (no note bodies in SQLite); the Notes Hub distinguishes every state (not-connected / empty / indexed / missing / error); Topic↔Note links are governed references that survive a rescan and a missing file, and a deleted/renamed file goes `missing / stale` **without ever touching the Knowledge Topic, its Evidence or its mastery** (`e2e/obsidian.spec.ts`). **Functional 85→88:** closes P1-3 (Obsidian V1 slice — a `CORE, APPROVED` integration that was entirely absent) with least-privilege filesystem access (canonical-root path safety, `..`/absolute/rooted/symlink-escape all rejected — security tests), and P1-17 (shared `EvidenceList` component adopted in 3 places + the explicit idempotent Development→Knowledge evidence handoff, with raw unreviewed AI-assisted evidence refused). Knowledge review gained an explicit "Mark reviewed" (schedule only, never mastery). Global Search now includes note metadata; Book `noteRef` reconciles against the index. **Visual/UX 70→72:** the Notes Hub goes from a one-line placeholder to a full operable surface on the locked tokens (scoped axe 0 critical/serious); the shared Evidence list unifies three previously hand-rolled lists. No global normalization (Batch 9). **Overall 79→82 (not higher):** real AI provider / recommendation Apply (Batch 6) is still the missing core-loop link, onboarding not persisted (Batch 7), resilience/loading states (Batch 8) and visual normalization (Batch 9) pending, and **native Tauri desktop verification stays BLOCKED** (external toolchain, rechecked at Batch 10). PBOS-authored Markdown writes and Fitness↔Evidence were confirmed out of Batch 5 scope. Migration **v7** (`obsidian_config` / `obsidian_notes` / `knowledge_topic_note_links` + 2 dangling-safe ALTER-added FK columns); v1–v6 untouched. Tests: vitest 550→**571** (+21), Playwright 32→**35** (+3), `cargo test` 71→**83** (+12). |
| 2026-08-31 | 4 | **79%** | **85%** | **70%** | **Master Batch 4 complete — the study EXECUTION → EVIDENCE stage of the operating loop is operable.** TOPIC → NORMAL STUDY → FOCUS → MASTERY CHECK → EVIDENCE → KNOWLEDGE MASTERY now runs end to end (`e2e/study.spec.ts`). **Functional 80→85:** Normal Study exists (mode-aware, explainable reason codes, no priority-score guess); Focus gained durable session history + study context (audit "no on-page history" closed) while still never granting mastery from time; Mastery Check + Result exist (deterministic self-check, neutral bands, no fake question bank); the Knowledge-evidence handoff is explicit and idempotent (`mastery_checks.evidence_id` set-once — one evidence row, re-record is a no-op), and a low result changes no grade / coverage / prior evidence. Closes the Normal Study / Mastery Assessment / Mastery Results screen gaps (3 of P1-2's 19), advances P0-4 (EXECUTION→EVIDENCE→DOMAIN-STATE for study now PASS) and P1-17 (one canonical evidence path + idempotency guard). Study modes reuse the **canonical** `settings.mode` — no competing store — and change ordering only (proven no data mutation). **Visual/UX 68→70:** three new operable surfaces (Normal Study, Mastery Check, Mastery Result) plus a Focus session history, on the locked tokens; the Mastery Check fieldset + radio groups pass scoped axe. No global normalization (Batch 9). **Overall 74→79 (not higher):** real AI provider / recommendation Apply (Batch 6) absent, Obsidian filesystem (Batch 5) absent, Development/Fitness execution depth and a shared Evidence component still pending, onboarding not persisted (Batch 7), resilience/loading states (Batch 8) and visual normalization (Batch 9) pending, and **native Tauri desktop verification stays BLOCKED** (external toolchain, rechecked at Batch 10). Migration **v6** (`focus_sessions` / `mastery_checks`); v1–v5 untouched. Past Papers (`docs/17`) confirmed out of Batch 4 scope. Tests: vitest 526→**550** (+24), Playwright 28→**32** (+4), `cargo test` 64→**71** (+7). |
| 2026-08-31 | 3 | **74%** | **80%** | **68%** | **Master Batch 3 complete — the PLAN stage of the operating loop is operable.** ACTION → PLANNER → CALENDAR → TODAY now runs end to end (`e2e/planning.spec.ts` "Plan → Calendar → Today"): an Action is scheduled into a canonical `PlanningBlock`, Calendar Week renders it, Today surfaces it grouped NOW/NEXT/LATER, and marking the Action Done is reflected on Today while the planning history survives — Action ≠ Planning Block ≠ completion held throughout. **Functional 71→80:** closes P0-2 (Planner/Calendar/Plan Builder existed nowhere → all operable: block CRUD, capacity editor, deterministic Generate→Review→Apply with manual/locked blocks surviving regeneration), P0-3 (Quick Capture broken/`/capture-inbox` 404 → durable `capture_inbox`, palette inline Confirm, canonical Action/Expense/Routine delegation with no duplicate writes, ambiguous capture survives reload), P1-7 (inbox now persisted), P1-10 (`/calendar` → real Calendar Week), P1-15 (Today NOW/NEXT/LATER + honest open-day state), P1-18 (Block + Capacity create/edit). P0-4 core loop: PLAN stage BROKEN → PASS; only USER DECISION → RE-PLANNING (AI apply, Batch 6) still missing. **Visual/UX 65→68:** Planner, Calendar Week and Capture Inbox are operable surfaces on the locked tokens; Plan Builder + Capacity forms use shared accessible `FormFields` (scoped axe 0 critical/serious); Today gains the grouped view + Day-17 open-day state. No global normalization (Batch 9) so the move is modest. **Overall 67→74 (not higher):** real AI provider / recommendation Apply (Batch 6) absent, Focus & Academic execution depth (Batch 4) and Obsidian filesystem (Batch 5) absent, onboarding not persisted (Batch 7), and **native Tauri desktop verification stays BLOCKED** (external `@wdio/tauri-service` / WebView2 151.x — accepted in Batch 2, rechecked at Batch 10). Migration **v5** (`planning_blocks` / `planning_capacity` / `capture_inbox`); v1–v4 untouched. Tests: vitest 502→**526** (+24), Playwright 24→**28** (+4), `cargo test` **64/64**. |
| 2026-08-31 | 2 (full) | **67%** | **71%** | **65%** | **Master Batch 2 product implementation complete** — Development, Fitness, Routines, Reading/Language and Money added to the already-committed Academics + Knowledge, so all 8 V1 configuration domains are now user-owned, relational and seed-free. **Functional 48→71:** audit §9 CRUD matrix closed for every remaining configuration entity — Project/Skill/Milestone (+ Project↔Skill many-to-many, provenance-weighted Evidence), Training Plan/Planned Session (+ the previously-absent ACTUAL Workout Session — closes P1-4), Recovery check-ins, Routine (+ schedule-aware consistency, Daily Check-In, System link), Language Path/Unit/Learning Session, Book/Reading Session, Money Transaction/Planned Expense/Budget/Savings Goal — all BROKEN/read-only/MISSING → create-edit-delete + persisted + tested. P0-1 (no create/edit UI for any config entity) substantially closed; P1-18 per-entity gaps closed except Block/Capacity (Batch 3). §3-D seed-as-user-data resolved for ALL 8 Batch 2 domains (fresh profile genuinely empty; every `mockData` reduced to test fixtures). §12 missing per-domain engines now PRESENT. Product locks proven with Rust+repo+RTL+Playwright evidence: Project≠Skill≠Knowledge, BasePlan≠Prescription≠ActualSession, Routine≠Action, LanguageSession≠KnowledgeMastery, ReadingProgress≠KnowledgeMastery, SavingsTransfer≠Expense, PlannedExpense≠ActualSpend, PBOSBalance≠BankBalance, Money≠PerformanceScore. **Visual/UX 60→65:** many MISSING/SHELL/PARTIAL screens across 5 domains are now operable management surfaces on the locked tokens + shared accessible `FormFields` (every new form: scoped axe 0 critical/serious); no global visual normalization (Batch 9) so it stays modest. **Overall 52→67 (not higher):** the core-loop PLAN stage (Planner/Calendar, Batch 3) is still missing, AI apply / real provider (Batch 6) absent, Obsidian filesystem (Batch 5) absent, and — weighting QA/resilience honestly — **native Tauri verification is BLOCKED** by an external toolchain incompatibility (product is proven at unit/engine/repo/Rust/RTL/Playwright levels but not yet through the packaged desktop binary). Tests: 307→**477** vitest (+170), Playwright 10→**24**, `cargo test` 18→**54**; native E2E still 4 executing (9 written). |
| — | 2A | **52%** | **48%** | **60%** | Academics + Knowledge are now user-owned, relational, and operable end to end. Audited gaps moved: §9 CRUD matrix — Academic Course C/U/archive, Academic Topic C/U/D, Assessment C/U/D, Attempt+marks, Knowledge Topic C/U/D, Knowledge Source C/U/D — all BROKEN/read-only → PASS. §12 "no Academic engine / no Knowledge engine as CRUD-backed" → PRESENT (relational model + validation engines + weighting analysis). §3-D (seed-as-user-data) resolved for both domains — fresh profile is genuinely empty, `mockData` reduced to test fixtures. Cross-domain: Academic Topic ↔ Knowledge concept proven with ONE ownership and zero duplicated mastery (Rust + TS + native-E2E evidence). Notes Hub exists and is honest (P1-3 PARTIAL). §6 screens: Academics Overview PARTIAL→operational, Course Detail SHELL→management surface, Course Builder MISSING→PASS, Knowledge Overview PARTIAL→operational, Topic Detail SHELL→management surface, Knowledge Topic Builder + Notes Hub MISSING→PASS. Tests: 231→307 (+76); Playwright 4→10; native E2E 3→4. **Not moved:** score→letter-grade + repeat-policy (still `RESEARCH REQUIRED`, deliberately unencoded); Obsidian filesystem read (Batch 5); Development/Fitness/Routines/Money/Planner CRUD (Batch 2B/3); AI apply (Batch 6); global visual normalization (Batch 9). |
